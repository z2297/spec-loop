# Decisions log — 20260626-web-dashboard

[intake] COUNCIL: ENDORSE_WITH_CONCERNS (effective) — verdicts: skeptic OBJECT(SAFETY), architect/pragmatist/guardian/historian ENDORSE_WITH_CONCERNS. The lone SAFETY OBJECT halted to the human per the aggregation rule; the human's up-front answers resolved it (scope, contract, base branch), converting it to endorse-with-concerns as the skeptic stated it would. All concrete concerns folded into the two-slice decomposition (below) and logged.
[intake] DECISION: scope = THIS REPO'S RUNS ONLY (glob docs/spec-loop/*/dag.json), per human — RATIONALE: "all local sessions" has no machine-wide registry; cross-repo scan is a separate, riskier, info-disclosure-prone feature; matches the terminal dashboard's discovery — REVERSIBILITY: moderate (multi-root is an additive follow-on).
[intake] DECISION: strictly READ-ONLY web viewer (GET/HEAD only, 127.0.0.1 bind, no mutation endpoints), per human — RATIONALE: preserves the existing dashboard's read-only "security boundary"; "interactive" = client-side nav/filter/auto-refresh — REVERSIBILITY: moderate.
[intake] DECISION: develop against the `alpha` branch (base_ref=alpha, base_sha=4125a1d), per human — RATIONALE: alpha (1.1.0-alpha.1) is the bleeding-edge channel and already contains the merged /spec-loop:dashboard markdown command that main lacks (prior run merged at 8f0a55c/12bfd65 on alpha/beta, never on main); developing here resolves that drift and lets the web dashboard reuse the existing command's contract — REVERSIBILITY: moderate. (The main-vs-alpha drift is thus handled by choosing alpha as base, NOT by bundling git-history recovery into a slice, per the Historian.)
[intake] DECISION: architecture = stdlib-Python http.server serving JSON API + a self-contained dark-theme single-page UI; "near real time" via client-side polling (~2-3s); NO websockets, NO node/npm/build tooling — RATIONALE: 4/5 council members converged on this as the only path that respects the repo's zero-third-party-dep, stdlib-Python (scripts/*.py) constitution while delivering the feature; polling is indistinguishable from real time for a wave-boundary-cadence source; file:// static HTML is a browser-sandbox dead end (cannot enumerate dirs / fetch file://) — REVERSIBILITY: moderate.
[intake] DECISION: decompose into 2 slices — s1 (Tier 2) read-only server + scan_runs data layer (reuses dashboard.md derivation rules verbatim; owns the security guardrails + tests + a minimal placeholder asset); s2 (Tier 1, deps s1) the full dark-theme polling frontend + a distinctly-named `dashboard-serve` launcher command + the full release ritual — RATIONALE: a reviewer could meaningfully reject the server/security layer while approving the UI/docs (and vice versa); s1 defines the JSON contract s2 consumes, so they serialize; release-ritual files (CHANGELOG/both READMEs/plugin.json) are owned by exactly one slice (s2) to avoid concurrent edits to shared files; s2 may SPLIT (frontend vs ritual) if the worker judges it oversized — REVERSIBILITY: trivial.
[intake] FOLDED council concerns (architect/guardian/historian/pragmatist): pure importable scan_runs separable from the HTTP handler (testable; single shared source of wave-derivation); path-traversal guard = enumerate-and-match + realpath-under-root; GET-only + 127.0.0.1 bind + Host-header allowlist (anti-DNS-rebinding); half-written dag.json -> 'unreadable' degrade, never crash/invent; ETag/304; serve frontend from a fixed bundled assets dir distinct from the data root; unit tests for scan_runs (incl. corrupt dag.json) + traversal/host/method rejection written first (validator doesn't lint scripts/*.py); reuse the exact dag.json contract + sibling artifacts (no second source of truth); full release ritual owed (CHANGELOG/both READMEs/plugin.json bump, marketplace.json untouched); distinct command name to avoid collision with /spec-loop:dashboard.
[intake] DEFERRED (logged, intentionally out of this run): machine-wide / cross-repo run discovery and multi-root config beyond this repo; any run-control (trigger/resume/cancel) from the browser — both are separate, separately-reviewed follow-ons.
[intake] NOTE: name `dashboard-serve` for the launcher command is a provisional, trivially-reversible choice (PROCEED+log); the slice worker/Iron Council may refine it as long as it does not collide with `/spec-loop:dashboard`.
[s1] IRON COUNCIL on plan: 4 ENDORSE_WITH_CONCERNS (skeptic/architect/pragmatist/historian) + 1 OBJECT (guardian, explicitly NOT SAFETY). Outcome: PROCEED with concerns folded (single non-SAFETY objection is not a majority and is a concrete, cheap, foldable plan-completeness gap, not "plan unworthy as written"). FOLDED: (guardian) static-asset path gets the SAME realpath-under-root confinement + traversal tests as the data path; os.sep/commonpath containment with root realpath'd (no docs/spec-loop-evil prefix collision); exact-string Host allowlist + run-id set membership (reject substring like localhost:PORT.evil.com); explicit symlink-escape + null-byte tests; bounded BYTES (per-file read cap, decisions-log tail cap) not just counts; generic 404 bodies with no path-existence oracle. (architect) ETag driven by If-None-Match — per-run digest for /api/runs/<id>, collection digest over all run mtimes for /api/runs; unreadable run by id -> 200 envelope not 404/500; Host check at top of do_GET before routing, status 421, handle absent Host; tests bind ephemeral port 0. (skeptic) data root = realpath(<root>/docs/spec-loop) resolved once at startup as the single trust boundary; Task-1 fixtures named as the acceptance criterion for derivation parity. (historian) json.dumps(ensure_ascii=False) preserved-unicode convention; test mirrors scripts' exit-code idiom. (pragmatist) keep decisions-log tail/request excerpt in detail payload for endpoint completeness; hold scope to --port/--root only.
[s1] RIGHT-SIZING: NOT split. Pragmatist verdict explicit — ONE independently shippable PR ("a curl-able read-only server"); scan_runs has no consumer without the HTTP layer and the guardrails are inseparable from the handler; splitting would strand scan_runs as dead code. depth=0 < cap=2 but the slice is correctly sized.
[s1] SCOPED REVIEW (Tier 2, pr-review-toolkit:code-reviewer, bar=MAJOR+): NO MAJOR/critical findings — security guardrails (method/host/traversal/symlink/oracle/bounds), derivation fidelity vs dashboard.md, and read-only contract all verified sound. 4 below-bar findings; applied 3 as cheap correctness/consistency wins (not required by bar, but improve source-of-truth fidelity): (1) ANSWERED escalation now honors dashboard.md Step 6's TWO signals — header `(status: ANSWERED)` OR a filled-in `Answer:` line — via per-block parsing so a partial write (Answer filled, header still OPEN) labels redispatch-pending not awaiting-human (+1 test, now 30 tests); (2) main() -> int annotation matching release.py/validate_marketplace.py; (3) resolve_within docstring corrected (it canonicalizes via realpath, does not string-scan for '..'). Finding #4 (detail-endpoint glob not MAX_RUNS-capped) NOT changed — bounded by filesystem, not attacker-amplifiable, asymmetry only; logged as accepted below-bar. No escalation; auto-fix budget untouched (zero at/above bar).
[s1] SIMPLIFY PASS (code-simplifier, non-blocking): 3 behavior-preserving clarity edits — extracted nested ternary in _parse_escalation_header to a _header_state() helper; _collection_material loop -> comprehension; index.html unreadable-run branch innerHTML -> DOM-building for consistency. Public signatures, JSON shapes, security guardrails, and test expectations untouched; 30/30 tests pass, validate_marketplace.py exit 0.
[s1] QUALITY GATE: PASS after 2 behavior-preserving refactor passes (budget 3). Initial measure flagged 4 fns over threshold: _label_slice (params=5, bundled the 3 derivation sets into one ctx dict + extracted _derive_label -> params=3); _derive_waves (cc>10, extracted _completed_ids/_pending_slices/_ready_ids/_without helpers -> worst-fn cc now 7); _send (params=5, introduced a Response namedtuple + _emit/_route so handler helpers return a Response, dropping per-call args); slice_obj test helper (params=6 -> **overrides kwargs). Final: all metrics under config thresholds (cc<=10, method<=50 lines, params<=4, nesting<=3, class<=300). 30/30 tests pass + validate_marketplace.py exit 0 -> behavior preserved. No threshold weakened; quality-gate.json untouched.

## [s2] Iron Council on plan — 2026-06-26
[s2] IRON COUNCIL on plan: 5/5 ENDORSE_WITH_CONCERNS (skeptic/architect/pragmatist/guardian/historian); 0 OBJECT, 0 SAFETY. Outcome: PROCEED with concerns folded. FOLDED: (architect, factual correction) detail endpoint — UNKNOWN id -> 404, KNOWN-but-half-written -> 200 {status:"unreadable"}; drill-down handles 404 (stale/removed run) distinctly from the unreadable card; ETag stored keyed PER-ENDPOINT (collection vs per-run etags differ) and reset on view switch; ONE re-armed setTimeout (not setInterval) re-pointed by navigation to avoid double-timer/cross-route races; single-file UI is the correct zero-dep/no-build fit (keep). (guardian, the one real risk on this frontend slice) XSS discipline on EVERY run-derived field incl. run_id/base_ref/sha/slice id/parent/deps/labels — textContent + createElement only, ZERO innerHTML/insertAdjacentHTML on untrusted data incl. polling repaint; derived labels mapped to CSS class via a FIXED allowlist so an unknown label can never become markup; unreadable-run {run_id,status}-only object guarded (no assuming counts/slices/base_ref). (historian) bump version via `scripts/release.py 1.1.0-alpha.2 --channel alpha` not hand-edit; command frontmatter mirrors dashboard.md incl. allowed-tools ["Bash"]; reuse :root palette incl. --split #a371f7; extend the placeholder's catch-block render for the reconnecting state. (skeptic) README scripts/ subtree is stale (missing dashboard_server.py/dashboard_assets/) — fix during the ritual; poll interval = one named constant POLL_MS=2500; freshness = "last successful check" (reset on 200 OR 304). (pragmatist) Task 1 reframed from "replace" to "EXTEND" the already-reviewed placeholder; keep freshness minimal (no extra 1s timer). (architect/guardian/historian) dashboard-serve read-only claim scoped to "mutates no run state" (launching a server IS a side effect), not "no side effects".
[s2] RIGHT-SIZING: NOT split (depth=0 < cap=2 but correctly sized). Pragmatist verdict explicit — ONE independently shippable change: the release ritual (CHANGELOG/two READMEs/plugin.json) is paperwork that DESCRIBES the frontend (shipping it without the UI announces a non-existent feature; shipping the UI without it leaves the feature undocumented/unversioned); the dashboard-serve launcher is a ~20-line markdown file pointing at the already-merged s1 server, too small to be its own slice and meaningless without the UI. The intake's permitted frontend-vs-ritual split fails the "independently rejectable" test (no reviewer accepts the ritual while rejecting the frontend) — inverse of s1, where the server was rejectable without any UI. Proceed whole.

## [s2] Scoped review (Tier 1) — 2026-06-26
[s2] SCOPED REVIEW (Tier 1, pr-review-toolkit:code-reviewer, bar=MAJOR+): NO MAJOR/critical findings. Verified the critical invariants: ZERO XSS (every run-derived field — run_id/goal/request_excerpt/decisions/escalation token+title/base_ref/base_sha/slice id/parent/deps/labels — reaches the DOM only via textContent/createTextNode through el(); zero innerHTML/insertAdjacentHTML/document.write/eval; derived labels -> CSS class only via the fixed LABEL_CLASS allowlist, unknown -> lbl-unknown, never raw-data className); strictly READ-ONLY client (single fetch, default GET, no POST/PUT/DELETE/PATCH/XHR/sendBeacon/WebSocket/EventSource, no mutating UI controls); polling correctness (single re-armed setTimeout, ETag keyed per-endpoint, 304->no repaint, detail-404->run-gone->overview, 421/5xx/unreachable->keep last render, unreadable {run_id,status} guarded); command convention (description+argument-hint+allowed-tools:["Bash"], honest read-only framing, distinct from /spec-loop:dashboard); release ritual (plugin.json alpha.1->alpha.2, CHANGELOG [Unreleased] Added, both READMEs, marketplace.json untouched). 3 below-bar findings; applied all 3 as cheap correctness/clarity wins (not required by the Tier-1 bar): (1) stale-in-flight-response guard — a viewGen epoch snapshot dropped at each await so a poll that resolves after navigation can no longer paint a detail payload into the overview (false "no runs") or vice-versa; (2) renderRunGone's deferred 1200ms auto-nav now tracked in goneTimer and cleared on enterView so manual nav within the window isn't overridden; (3) corrected the misleading post-renderRunGone comment (finally re-arms). Re-verified after fix: validator exit 0, 30/30 s1 tests, page serves 200 with both runs, node --check JS syntax OK. No escalation; auto-fix budget untouched (zero at/above bar).

## [s2] Simplify pass + Quality gate (Tier 1) — 2026-06-26
[s2] SIMPLIFY PASS (code-simplifier, non-blocking): 2 behavior-preserving clarity edits to scripts/dashboard_assets/index.html — removed a dead `.run .meta .recency` CSS rule (no JS path produced a .recency node) and collapsed a needless intermediate variable in pill() to a direct return. All invariants preserved (zero innerHTML/insertAdjacentHTML on data, GET-only read-only client, LABEL_CLASS allowlist + lbl-unknown fallback, --split palette). Re-verified: validator exit 0, 30/30 s1 tests, page serves 200, node --check JS syntax OK.
[s2] QUALITY GATE (Tier 1): PASS after 1 behavior-preserving refactor pass (budget 3). Changed code is the vanilla JS in index.html (the other 5 changed files are markdown/JSON docs/metadata — no functions/classes, gate no-ops). Initial measure: all functions under thresholds EXCEPT poll() at cyclomatic complexity ~14 (> threshold 10) — the inline status-branch chain plus the triple repeated `gen !== viewGen` stale-guard. Refactor: extracted fetchOpts() (header/If-None-Match build), applyResponse() (the 304/detail-404/2xx/else status branching incl. the post-json gen re-check), and repaint() (view dispatch) out of poll(); poll() now only orchestrates fetch + the gen-snapshot guards + finally/arm. Final metrics: poll cc=5, applyResponse cc=7, fetchOpts cc=2, repaint cc=3 (all <=10); worst method=17 lines (<=50), worst nest=2 (<=3), worst params=3 (<=4); no classes (class_lines n/a). Behavior preserved — identical 304-no-repaint / detail-404->run-gone / unreachable-keep-last-render / stale-poll-drop semantics; re-verified live: page 200, detail 200/404, 304 path all correct, 30/30 s1 tests, validator exit 0, JS syntax OK. No threshold weakened; quality-gate.json untouched. CRAP skipped (no coverage tooling for browser JS in this zero-dep repo — expected, noted).

## [run] Phase 3 wave boundaries — 2026-06-26
- s1 DONE VERIFIED by controller: merge 1f83de4 on alpha (4125a1d..1f83de4 = scripts/dashboard_server.py +532, test_dashboard_server.py +400, dashboard_assets/index.html +113); 30/30 tests + validate_marketplace.py exit 0 on merged alpha; live smoke: /api/runs surfaced both repo runs with correct DERIVED labels, traversal->404, foreign Host->421, POST->405; worktree cleaned. s1 flipped pending->complete.
- s2 DONE VERIFIED by controller: merge b04b76f on alpha (1f83de4..b04b76f = 6 declared files; index.html rewritten +490/-59; dashboard-serve.md +63; CHANGELOG/both READMEs; plugin.json 1.1.0-alpha.1->1.1.0-alpha.2 via release.py); marketplace.json untouched; new command frontmatter has required `description`; s1's server/test files NOT in the diff (30/30 still pass); worktree cleaned. s2 flipped pending->complete.

## [run] Phase 5 integration gate — 2026-06-26
- Step 1 (full suite on assembled alpha): python3 scripts/test_dashboard_server.py -> 30/30 OK; python3 scripts/validate_marketplace.py -> exit 0. GREEN.
- Step 2 (cross-slice integration): the genuine seam is s2-frontend <-> s1-API JSON contract. Verified LIVE (server up, GET / -> 200, self-contained dark theme + inline JS calling /api/runs; /api/runs/<id> returns full envelope {base_ref,base_sha,counts,decisions_tail,open_escalations,request_excerpt,run_id,slices,waves}, slice keys {deps,depth,files,goal,has_report,id,label,parent,risk_tier,status,subsystems}) AND statically (every key index.html reads is emitted by dashboard_server.py; no missing-key mismatch). GREEN.
- DECISION: substituted the targeted live+static contract verification for a second full review-pr at Tier 2 — RATIONALE: s1 (Tier 2) and s2 (Tier 1) each already passed tier-appropriate review with no MAJOR+ findings; the only cross-slice contract is the API shape, now verified directly end-to-end; a redundant review-pr over the same cumulative diff adds no new signal (same reasoning as run 20260625-dashboard-cmd Phase 5) — REVERSIBILITY: trivial.
- Integration gate GREEN. No remediation slice needed. Run complete. NOTE: all work merged to LOCAL alpha (1f83de4, b04b76f) — not pushed (no push requested). docs/spec-loop/ run-state remains untracked, consistent with repo layout.
