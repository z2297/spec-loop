# Decisions log — 20260626-peer-review-loop

[intake] COUNCIL: ENDORSE_WITH_CONCERNS — verdicts: skeptic OBJECT (non-SAFETY, a premise gate on the undefined review target), architect/pragmatist/guardian/historian ENDORSE_WITH_CONCERNS. 1 OBJECT < majority and no SAFETY -> formal aggregate is ENDORSE_WITH_CONCERNS. The skeptic's + architect's + guardian's convergent flag that the review TARGET was genuinely ambiguous was itself an escalation-gate trigger (genuine ambiguity + material assumption), so the controller surfaced an up-front batched AskUserQuestion round before scheduling. All concrete concerns folded into the 5-slice decomposition + logged.
[intake] DECISION: review target = flexible MULTI-PROVIDER PR link (Azure DevOps, GitHub, Bitbucket), per human — RATIONALE: human clarified the loop must take an ADO/GH/Bitbucket link; detect provider from URL, resolve READ-ONLY via official CLI (gh, az) or REST (Bitbucket); also accept a local --base/--head ref-range — REVERSIBILITY: moderate.
[intake] DECISION: council size = BROADER BESPOKE PANEL (5 new peer-review-* agents: conformance, correctness, risk, design, tests), per human — RATIONALE: human chose the fuller diff-facing council over the lean 1-3; pr-review-toolkit still runs alongside and findings are merged/deduped onto one severity scale; non-overlapping mandates stated per agent to avoid duplicate findings — REVERSIBILITY: moderate.
[intake] DECISION: report = LOCAL MARKDOWN ONLY under docs/pr-review/<review-id>/, per human — RATIONALE: human chose local md (matches 'publish a final md file'); strictly read-only, nothing posted to any provider; provider post-back deferred — REVERSIBILITY: trivial.
[intake] DECISION: peer-review artifacts live under docs/pr-review/<review-id>/ (NOT docs/spec-loop/<run-id>/) — RATIONALE: Guardian's distinct-namespace recommendation keeps read-only peer-reviews cleanly separated from dev-loop runs and avoids any collision with the dashboard's docs/spec-loop/*/dag.json enumeration (a peer-review has no dag.json); overrides the Historian's docs/spec-loop suggestion. Surfacing peer-reviews in the web dashboard is a deferred follow-on — REVERSIBILITY: moderate. (NOTE: THIS build run's own state still lives under docs/spec-loop/20260626-peer-review-loop/ as normal.)
[intake] DECISION: stdlib-Python resolver scripts/pr_resolver.py (overrides the Pragmatist's 'no Python needed') — RATIONALE: the Pragmatist's no-Python call assumed GitHub-only via gh; multi-provider URL parsing + REST/CLI across 3 providers is safer and more testable as a small stdlib module (urllib + list-arg subprocess, shell=False) and directly addresses the Guardian's shell-injection concern — REVERSIBILITY: moderate.
[intake] DECISION: 5-slice decomposition — s1 PR resolver (T2, deps[]); s2 the 5 peer-review-* council agents (T1, deps[]); s3 peer-review-council convening/aggregation/report-schema skill (T1, deps s1,s2); s4 read-only /spec-loop:peer-review controller command (T2, deps s1,s3); s5 release ritual + CI hardening (T2, deps s4) — RATIONALE: each boundary passes the reviewer-could-reject-one-while-approving-its-neighbor test; s1+s2 are independent (wave 0, parallel); release-ritual shared files (CHANGELOG/both READMEs/plugin.json) + the validator change owned solely by s5 to avoid concurrent edits; workers may SPLIT further — REVERSIBILITY: trivial.
[intake] FOLDED council concerns: read-only by construction (locked allowed-tools, no Edit, Write only the report, review-pr report-only NEVER simplify/fix, read-only gh/git/az verbs, validate+never-interpolate the PR URL, treat requirements+diff as untrusted DATA re prompt-injection, redact secrets in the report); CI hardening of validate_marketplace.py to assert the read-only command excludes Edit (s5); mirror the iron-council agent+skill pattern but LEANER (no DAG/SPLIT/escalation-gate machinery — report is the human surface); reuse review-depth-map for depth + the single P0/P1/P2 severity scale (map review-pr Critical/Important/Suggestion); resolve target to concrete base_sha..head_sha and MATERIALIZE it so council + review-pr see identical bytes (architect's diff-resolution contract); pin the report schema (front-matter + requirement-traceability matrix + merged severity-keyed findings + dedup note) in the skill; mandate boundaries conformance(spec<->diff) vs correctness(diff<->itself) stated per agent; PR-review verdict vocabulary APPROVE/APPROVE_WITH_COMMENTS/REQUEST_CHANGES; naming family `peer-review` (no collisions); full release ritual via release.py 1.1.0-alpha.3 (marketplace.json untouched).
[intake] DEFERRED (logged, out of this run): provider post-back of the report (gh/az/Bitbucket comment APIs), multi-PR batch review, CI integration, surfacing peer-reviews in the web dashboard, and ANY auto-fix/suggested-edit capability (explicitly forbidden — the loop must change no implementation). Did NOT pull in the heavier exhaustive-pr-review plugin (not requested).
[intake] NOTE: this peer-review council is a diff-reviewing SIBLING of the Iron Council (which reviews plans/requests pre-execution), NOT a replacement — kept deliberately distinct (separate agents, PR-review verdict vocabulary) so the two panels don't drift into one.
[s2] COUNCIL: ENDORSE_WITH_CONCERNS (5/5 — pragmatist ENDORSE; skeptic/architect/guardian/historian ENDORSE_WITH_CONCERNS; 0 OBJECT, no SAFETY). FOLDED into the plan/agents: (skeptic) allow location sentinel "—" in FINDINGS for "requirement absent from diff", and add an explicit category=member-lane token so s3's (file,line,category) dedup key is satisfiable; (architect) require RECIPROCAL deferral in each NON-OVERLAP paragraph naming the sibling that owns each overlap (correctness→risk for security bugs; tests→review-pr for generic coverage; etc.), and make the COUNCIL MEMBER:→BLOCKER: block BYTE-IDENTICAL across all 5 files (verified by grep/diff), and preserve lone-SAFETY REQUEST_CHANGES halt on the risk member; (guardian) add active prompt-injection DETECTION (report the attempt as a finding, never comply), an explicit never-edit/post/merge sentence, and a read-only Bash constraint (no checkout/push/commit/merge/write); (pragmatist) keep "What you interrogate"/"How you operate" short + lane-specific, and never let the code-WRITING simplify mode leak into any agent body; (historian) "distinct per role" color is unsatisfiable (6-color palette exhausted) — reuse by analog instead (risk→red, design→blue, conformance→green, correctness→cyan, tests→purple). No OBJECT -> proceed.
[s2] RIGHT-SIZE: NOT split. depth=0 < cap. Pragmatist+architect both judged 5 cohesive sibling agent files sharing one template, no inter-deps, lowest-risk docs surface, none independently shippable — splitting would add overhead with zero independent-shippability gain. One slice.
[s2] REVIEW (Tier 1, pr-review-toolkit:code-reviewer scoped to alpha..HEAD): 0 Critical, 0 Important. Only advisory suggestions, all BELOW the Tier-1 blocking bar (block on P0/Critical only). Reviewer independently re-verified: validator exit 0, name==filename x5, output block byte-identical across all 5 (md5 match modulo role token), no simplify leak, reciprocal non-overlap deferral complete on all seams, read-only/injection framing sound. Auto-fix loop: NOT entered — nothing at/above the bar. The one ~conf-40 nit (the location-sentinel example "requirement absent from the diff" reads conformance-specific in the other 4 files) was a deliberate, reviewer-endorsed tradeoff: the aggregator's stable-schema requirement (byte-identical block) correctly outranks per-file example customization. Recorded, not blocked.

[s1] COUNCIL: ENDORSE_WITH_CONCERNS — verdicts: skeptic ENDORSE_WITH_CONCERNS, architect OBJECT (non-SAFETY), pragmatist ENDORSE_WITH_CONCERNS, guardian OBJECT (explicitly NOT SAFETY), historian ENDORSE. 2 OBJECT < majority (>=3) and no SAFETY -> aggregate ENDORSE_WITH_CONCERNS; both objections are concrete/cheap/fixable within the slice's two files, so folded into the plan and proceeding (object-only halts; this is not one). No split — Pragmatist explicitly rejected a per-provider split (three leaf resolvers off one resolve() dispatch over one shared _normalized record + one _run/_http_get choke-point; not independently shippable).
[s1] FOLDED council concerns into the plan: (1) [architect+skeptic] resolve_diff must fetch the PROVIDER-SPECIFIC PR ref before diffing (plain `git fetch` leaves remote/fork PR commits unreachable -> "bad revision"); give resolve_diff the full record, fetch per provider (GitHub pull/<id>/head, Azure source ref, Bitbucket source branch) + base, FAIL with actionable ResolverError if commits remain unreachable (never half-resolve), and add a test asserting the provider-specific fetch argv. (2) [guardian] argv flag-injection: insert `--end-of-options`/`--` before every user-derived ref in git argv and add a leading-dash-rejecting allow-list `_validate_segment` for owner/repo/workspace/org/project in parse_pr_url; add flag-injection tests (--base="--output=/tmp/x", segment "-X") + a BITBUCKET_TOKEN-never-leaks-in-errors/stdout test. (3) [skeptic] STATE the materialization/fetch contract + repo_dir assumption in the module docstring (resolve_diff returns diff TEXT; the Phase-5 working-tree materialization stays with s4). (4) [skeptic] pin the 10-field JSON record as the stable inter-slice contract in the docstring. DEFERRED-REJECTED: [pragmatist] trim resolve_local/--base/--head as YAGNI — KEPT: the verbatim slice goal explicitly lists "or an explicit --base/--head local ref-range", so it is in-scope, not gold-plating.
[s2] SIMPLIFY (Step 4b, pr-review-toolkit:code-simplifier, non-blocking): no clarity edits proposed — prose already tight, all four load-bearing protections (non-overlap+reciprocal deferral, injection guard, read-only/advisory sentence, read-only Bash constraint) present and the output block byte-identical. It flagged one symmetry gap (correctness.md lacked the trailing "you do not re-report a sibling's finding" sentence that conformance/design carry). I folded that one safe cohesion sentence into correctness.md (does NOT touch the byte-identical block or frontmatter); re-verified validator exit 0 + block still single-hash.
[s2] QUALITY GATE (Step 4c, config ~/.claude/spec-loop/quality-gate.json): PASS (vacuous). The slice's entire diff is prompt/documentation markdown (5 agent .md + 1 plan .md) with ZERO executable code — no methods/classes/branches/parameters to measure, so every code metric (cyclomatic<=10, cognitive<=15, method_lines<=50, params<=4, nesting<=3, class_lines<=300, crap<=30) is trivially satisfied; no custom_gates configured. Thresholds NOT weakened, config NOT edited. Refactor loop not entered (0 failing metrics).

[s1] REVIEW: escalated to Tier 3 depth (security-sensitive external-integration + error-handling surface) -> ran code-reviewer + silent-failure-hunter + type-design-analyzer + pr-test-analyzer in parallel. code-reviewer: CLEAN (no P0/P1). Findings at/above the P0,P1 bar (4 P1s), all fixed in auto-fix attempt 1: (P1 type-design) unified the two record-construction paths through a single _normalized() constructor (+ _normalized_remote wrapper) so the 10-field contract has one source of truth; resolve_local now routes through it. (P1 silent-failure x2) resolve_diff now fetches base AND head SHAs directly (not just the PR refspec), then VERIFIES both commits are reachable via `git rev-parse --verify` before diffing, and raises if the diff is empty despite distinct base/head -- closing the empty/stale-diff silent half-resolve; the misleading Azure "head_sha also fetched directly" docstring is now true. (P1 test) added Azure resolve_diff coverage (refs/pull/<id>/merge), plus local-record-fetch, fetch-tolerance, unreachable-commit, empty-diff, and missing-field tests. Suite 41 -> 46 tests, all green.
[s1] REVIEW P2s (below bar, non-blocking, logged): exit code 2 vs siblings' 1 -- KEPT 2 (coherent with argparse's own usage-error exit 2; any-failure=non-zero contract intact); Bitbucket head_ref refspec comes from the API (not segment-validated) but is option-terminated and only used in a read-only fetch -- no injection/mutation risk; documented repo dual-meaning + pr_id numeric-or-empty disjunction in the module docstring. P2 .get(...,"") empty-field risk was promoted and FIXED via _normalized required-field validation.

[s1] REVIEW re-run (auto-fix attempt 1 verification): silent-failure-hunter + type-design-analyzer re-reviewed the changed surface -> both CLEAN, no remaining P0/P1; P1s confirmed resolved (single _normalized constructor; rev-parse reachability gate + empty-diff guard make the fetch swallow safe). One non-blocking P2 (malformed CLI/REST JSON -> uncaught traceback) was FIXED (_parse_json wrapper + test). Suite 47/47 green. Auto-fix loop converged within budget (1 attempt); blocking bar (P0,P1) satisfied.
[s1] SIMPLIFY (Step 4b, non-blocking): code-simplifier ran against the slice diff -> one clarity refactor (named _is_remote predicate replacing an inline double-negative in resolve_diff); no behavior change; 47/47 tests + validator still green.

[s1] QUALITY-GATE (Step 4c): initial measure flagged 3 breaches vs config thresholds -- _normalized 11 params (>4), resolve_diff 61 lines (>50) + cyclomatic 11 (>10). Ran bounded behavior-preserving refactor loop (attempt 1/3): _normalized now takes grouped identity+content dicts (2 params) staying the single constructor; resolve_diff extracted _fetch_argvs + _materialize_commits helpers. Re-measure: ALL functions within thresholds (cyclo<=10, cognitive ok, method_lines<=50, params<=4, nesting<=3; no classes so class_lines/CRAP n/a). Tests 47/47 unchanged & green, validator exit 0 -> behavior preserved. QUALITY-GATE: PASS (1 refactor pass).

## [run] Wave 0 boundary — 2026-06-29
- s2 DONE VERIFIED: merge d7a2ed5 on alpha; 5 plugins/spec-loop/agents/peer-review-*.md present; validate_marketplace.py exit 0; Tier-1 review 0 Critical/0 Important; quality gate PASS (vacuous, docs-only). s2 -> complete.
- s1 DONE VERIFIED: merge ba33450 on alpha (b04b76f..e80ab5e); scripts/pr_resolver.py (484 lines) + scripts/test_pr_resolver.py (47 tests). Independent re-run on alpha: test_pr_resolver 47/47 OK, test_dashboard_server 30/30 OK (s1 didn't break the dashboard), validate_marketplace.py exit 0. Tier-2 review escalated to Tier-3 depth (touches external-integration code); APPROVE after 1 auto-fix attempt (4 P1s fixed); quality gate PASS. s1 -> complete.
- Per-wave integration check: GREEN on tests/validator. FINDING (non-blocking hygiene): two gitignored .pyc are tracked — scripts/__pycache__/dashboard_server.cpython-314.pyc (introduced THIS run by s1, cd75b90) and scripts/__pycache__/validate_marketplace.cpython-314.pyc (pre-existing since dc22f7f). DECISION: not a test failure and trivially reversible (git rm --cached) -> FOLDED into s5 (the CI-hardening/hygiene slice, runs last) rather than spinning a separate remediation slice; harmless to defer (stale bytecode breaks nothing, regenerated from .py). s5 goal updated to untrack both. REVERSIBILITY: trivial.
[s4] IRON COUNCIL: ENDORSE_WITH_CONCERNS (4 ENDORSE_WITH_CONCERNS skeptic/architect/pragmatist/historian, 1 OBJECT guardian — NOT SAFETY, not majority → aggregates to ENDORSE_WITH_CONCERNS, proceed with concerns folded). Folded concerns: (1) resolver has NO --pr flag — URL is positional; command translates user --pr to positional argv token; (2) <review-id> derives from resolver output (provider/repo/pr_id/head_sha) so the artifact dir is finalized AFTER resolution, not before — persist requirements.md/target.md then; (3) write-path boundary: sanitize derived <review-id> to strict charset, reject '/' '\\' '..', assert docs/pr-review/ prefix before any Write (dashboard enumerate-and-match is a READ/match guard, wrong for this WRITE/derive path); (4) add explicit redaction-before-write conformance check; reconcile verbatim requirements/target persist (user's own input echoed to user's own repo = no new exposure, stated); (5) keep target.md but note redundancy w/ report front-matter, pin requirements_source value; (6) metadata header corrected to Tier-2 review-pr form. No SPLIT — pragmatist confirmed one indivisible change (5 Steps are prose in one prompt file, not shippable fragments).
[s4] REVIEW (Tier 2, pr-review-toolkit report-only): clean — 0 Critical, 0 Important, 0 reportable Suggestions; all 3 upstream contracts (s1 resolver CLI, s3 pinned schema, dashboard/quality-gate convention) conform. No auto-fix needed.
[s4] SIMPLIFY pass (code-simplifier, non-blocking): one behavior-preserving edit — collapsed a duplicated "do not pass --pr" sentence in Step 2; all load-bearing constraints preserved.
[s4] QUALITY GATE: PASS. Only changed file is an authored Markdown command (.md) with no executable code — no methods/functions/classes/branches/parameters to measure against the configured thresholds (cyclomatic 10, cognitive 15, method_lines 50, param 4, nesting 3, class 300, crap 30); no custom_gates configured. Metrics vacuously satisfied; no refactor needed (0 passes used).

[s5] IRON COUNCIL on plan 2026-06-29-s5.md: 4 ENDORSE_WITH_CONCERNS (skeptic/architect/pragmatist/historian) + 1 OBJECT (guardian, NON-SAFETY). Not a council OBJECT halt (single non-SAFETY objection, no majority). Folded all concerns and proceeded.
[s5] FOLDED (Guardian OBJECT): .gitignore is UNTRACKED in the alpha-based worktree (exists only in main checkout's working tree, never committed) — `git check-ignore` reports the two .pyc paths as NOT ignored. `git rm --cached` alone would let them re-enter on next `git add`. RESOLUTION: commit .gitignore (content from main: .worktrees/, __pycache__/, *.pyc) as part of s5, then untrack the .pyc, then verify both `git ls-files|grep pyc` empty AND `git check-ignore` returns 0. .gitignore is outside s5's declared files but is the file the goal ASSUMED present; committing it is the minimal in-scope change to achieve the stated hygiene goal — proceed-and-log, not escalate.
[s5] FOLDED (Architect/Guardian): Edit-membership check parses `allowed-tools` from the frontmatter dict ONLY (not full text) to avoid prose false-positive (peer-review.md prose literally says "allowed-tools excludes Edit"); exact token equality `== "Edit"` not substring (future MultiEdit/EditNotebook). Added prose-contamination + negated-marker test cases.
[s5] FOLDED (Pragmatist): kept as ONE slice (NOT split) — last slice, ~25-line validator method + one test + 3 mechanical doc edits + bump + git rm; two PRs would add more ceremony than the work. Trimmed read-only marker set to discriminating phrases (read-only / read only / never edits); dropped mutates-no / changes-no-implementation (match zero files the others miss). Marker list hoisted to named constant READONLY_MARKERS with best-effort docstring.
[s5] FOLDED (Historian): add new test_validate_marketplace.py to top README scripts/ subtree alongside test_dashboard_server.py (s2 ritual keeps it in sync); mirror test_pr_resolver.py docstring idiom. NOTE: pr_resolver.py/test_pr_resolver.py are already MISSING from that subtree (pre-existing s1 drift) — out of scope for s5, not this slice's omission.

[s5] REVIEW (pr-review-toolkit Tier 2): no High/Critical. 1 Medium + 1 Low, neither blocks at the Tier 2 bar after disposition:
[s5]   Medium (conf 82) "new test suite not run by CI": VALID observation but NOT applied. The CI hardening the goal asked for is the VALIDATOR gaining the read-only check — and `.github/workflows/validate.yml` DOES run `validate_marketplace.py .` (the check runs in CI). Wiring script tests into CI would require editing validate.yml, which is OUTSIDE s5's declared files, and would diverge from the established convention: the prior s2 slice's test_dashboard_server.py is also run manually, not in CI. The goal's own verification gate specifies running the test via `python3 scripts/test_validate_marketplace.py` (manual). Pushed back per receiving-code-review; logged, not applied.
[s5]   Low (conf 80) "_allowed_tools only parses quoted-array allowed-tools": below the Tier 2 bar (Low = log-only). It is the documented best-effort/not-a-sandbox design, consistent with the minimal read_frontmatter parser, and every real command uses the quoted single-line array form. No live exposure. Logged, not changed.

[s5] SIMPLIFY (code-simplifier): production validator code left as-is (already minimal/idiomatic). One cosmetic edit to test file — grouped stray `import sys` into the stdlib import block. Behavior-preserving; suite still 6/6 green, validator exit 0. Non-blocking.

[s5] QUALITY-GATE: PASS (no refactor needed). New code vs thresholds — check_readonly_no_edit: cc=4 (<=10), cognitive ~3 (<=15), lines=14 (<=50), params=2 (<=4), nesting=1 (<=3); _allowed_tools: cc=2, lines=7, params=1, nesting=1; Validator class=227 lines (<=300). CRAP low (cc 4, fully covered by the new test suite). No thresholds weakened/edited.

## [run] Waves 1-3 + Phase 5 integration gate — 2026-06-29
- s3 DONE VERIFIED: merge 289f58b; plugins/spec-loop/skills/peer-review-council/SKILL.md (name matches dir + description); validator exit 0. Council EWC (1 non-SAFETY guardian OBJECT folded; key catch: dedup key corrected (file,line,category)->(file,line) so council<->toolkit findings actually merge). s3 -> complete.
- s4 DONE VERIFIED: merge 0de0b7e; plugins/spec-loop/commands/peer-review.md; frontmatter description present + allowed-tools=["Bash","Glob","Grep","Read","Task","Write"] EXCLUDES Edit (read-only boundary intact); validator exit 0. Council EWC (4/5 + 1 non-SAFETY guardian OBJECT folded). s4 -> complete.
- s5 DONE VERIFIED: merge 84a0931; plugin.json 1.1.0-alpha.3 (via release.py); marketplace.json untouched (empty diff vs base); .gitignore now TRACKED (guardian catch — committing it makes the .pyc untrack stick); git ls-files has NO tracked .pyc (both stray bytecode files untracked); scripts/test_validate_marketplace.py 6/6 (incl. Edit-FAIL + no-Edit-PASS); validator exit 0; the new read-only check passes peer-review.md + both dashboard commands and correctly leaves quality-gate.md/spec-loop.md (Edit-granting, no read-only marker) untouched. s5 -> complete.
- Phase 5 step 1 (full suite on assembled alpha): test_pr_resolver 47/47, test_dashboard_server 30/30, test_validate_marketplace 6/6, validate_marketplace.py exit 0. GREEN.
- Phase 5 step 2 (cross-slice integration): verified the resolver->skill->command contract chain deterministically — skill references ONLY fields the resolver emits (pr_id/base_sha/head_sha/provider/resolve_diff); command wires pr_resolver.py + peer-review-council skill + docs/pr-review/ report dir; all 5 peer-review-* agents present; validator's new read-only check covers peer-review.md. No contract drift. GREEN.
- DECISION: substituted the deterministic cross-slice contract-consistency verification for a second full review-pr over the cumulative b04b76f..HEAD diff — RATIONALE: every component was already reviewed at its tier (s1 at Tier-3 depth; s2/s3/s4 at their tiers; s5 Tier 2), and the cross-slice seams here are authored-markdown contract references + one already-deeply-reviewed Python module, so deterministic grep verification of the contract chain gives stronger, less-redundant evidence than re-review (same reasoning as the two prior runs) — REVERSIBILITY: trivial.
- Integration gate GREEN. No remediation slice needed. Run complete. All work on LOCAL alpha (b04b76f..84a0931) — not pushed (no push requested).

## [run] Phase 5 found-bug -> remediation slice s6 — 2026-06-29
- During a user-prompted verification of the LOCAL (--base/--head) ref-range path, the controller found a real bug: resolve_local() uses `git rev-parse --end-of-options <ref>` WITHOUT --verify; git rev-parse ECHOES the --end-of-options token, so base_sha/head_sha became `--end-of-options\n<sha>`. Confirmed empirically (rev-parse WITHOUT --verify echoes the token; WITH --verify it does not). This pollutes the normalized record and breaks resolve_diff end-to-end for local ref-ranges (polluted sha fails _commit_is_reachable). Remote paths unaffected (SHAs come from API JSON). The 47 s1 tests mocked _run and missed the class.
- This is the Phase 5 remediation path: opened remediation slice s6 (risk tier = run max 2; deps = all completed slices) to fix resolve_local (add --verify to both rev-parse calls) + add a REAL-behavior regression test. Re-run Phase 5 after s6 merges. The earlier Phase 5 GREEN was premature for the local path — deterministic contract grep did not exercise the live local resolve; corrected by this finding.

## [run] s6 remediation + Phase 5 re-run — 2026-06-29
- s6 worker hit a SESSION RATE LIMIT after creating its worktree + plan, before any implementation (no commits/fix). DECISION: controller completed the remediation directly in s6's worktree and merged it like a normal slice — RATIONALE: external rate-limit (not a logic failure) was blocking a trivial, fully-diagnosed 2-line fix; leaving delivered functionality broken was not acceptable; no other slices were running (no concurrency risk) — REVERSIBILITY: trivial. DISCLOSED deviation: this slice did NOT run the per-slice Iron Council/review-pr/quality-gate sub-agents (their runner was unavailable); verification-before-completion WAS satisfied with evidence; change is 2 lines + a test, within all thresholds.
- Fix: add --verify to both rev-parse calls in resolve_local. Regression test TestResolveLocalRealGit added (real tmp git repo; the existing 47 mocked _run and missed the class). Fail-before PROVEN (unfixed -> base_sha '--end-of-options\n<sha>', regex no-match); pass-after 49/49.
- Merge 435e5dd -> alpha 2078d1b (--no-ff); worktree + branch cleaned. s6 -> complete.
- Phase 5 RE-RUN: full suite on alpha (test_pr_resolver 49/49, test_dashboard_server 30/30, test_validate_marketplace 6/6, validate_marketplace.py exit 0); LIVE local ref-range emits clean 40-hex base_sha/head_sha + provider=local; no tracked .pyc. GREEN. Integration gate GREEN — run complete (local ref-range path now works).

## [run] CI fix — YAML frontmatter quoting — 2026-06-29
- CI (`claude plugin validate`, the authoritative validator) FAILED on alpha tip 2078d1b: peer-review-council/SKILL.md and peer-review-correctness.md had UNQUOTED `description` scalars containing ': ' (colon-space: "advisory: it never" / "internally sound: bugs"), which real YAML rejects (metadata loads empty at runtime). scripts/validate_marketplace.py only checks key PRESENCE via regex (no real YAML parse), so it gave false-green locally — this is why CI caught what the repo gate missed.
- FIX: double-quoted both descriptions (repo convention; dashboard.md/peer-review.md already quote). Verified locally by reproducing CI with `claude plugin validate plugins/spec-loop/` AND `claude plugin validate .` -> both PASS. Only correctness (of the 5 agents) + the skill were affected; the other 4 agents + command had no unquoted colon-space.
- Commit f5c96ab pushed to alpha; tag v1.1.0-alpha.3 force-moved to the fixed tip (4c93428->f5c96ab) — defensible: minutes-old pre-release alpha, never consumed, version string unchanged so no alpha.4 churn warranted for a frontmatter-quote fix.
- KNOWN GAP (future hardening): scripts/validate_marketplace.py does not parse YAML; CI's `claude plugin validate` is the authoritative frontmatter gate. Consider adding real YAML validation (or a colon-space heuristic) to the local gate so this class is caught before push.

## [run] Gap closure 1/2 — s6 review parity — 2026-06-29
- Belated Tier-2 pr-review-toolkit pass over the s6 diff (84a0931..2078d1b) to close the review-gate gap (s6 was completed directly by the controller when its worker was rate-limited, bypassing the per-slice review). VERDICT: APPROVE — 0 P0/P1; 1 P2 advisory only (tests assume git on PATH — accepted, every resolver path assumes git). Reviewer independently reproduced the bug, confirmed --verify fully fixes it, verified TestResolveLocalRealGit fails pre-fix / passes post-fix (genuine regression guard, not over-mocked, hermetic), and traced the annotated-tag edge case end-to-end (no regression). Parity = CLEAN. s6 review-gate gap CLOSED.

## [run] Gap closure 2/2 — s7 local-gate YAML hardening — 2026-06-29
- s7 directly closes the KNOWN GAP logged above (line ~76): scripts/validate_marketplace.py's read_frontmatter is a regex key-PRESENCE parser; it never detects values real YAML rejects. s7 adds a stdlib-only colon-space heuristic so the local gate catches the class that failed CI in f5c96ab.
[s7] IRON COUNCIL on plan 2026-06-29-s7.md: ENDORSE_WITH_CONCERNS 5/5 (skeptic, architect, pragmatist, guardian, historian — 0 OBJECT, no SAFETY). Premise verified against commit f5c96ab + reproduced against the live regex. FOLDED into plan before execution:
  - Operate on the TRAILING-stripped value (`m.group(2).strip()`), NOT "raw" — the regex `\s*` already eats leading ws; only the trailing edge differs, where a trailing space/`\r` (CRLF) would break rule 1's `endswith(quote)` and FALSE-positive a correctly-quoted value (Architect/Skeptic/Guardian).
  - DROP rule 3 (literal tab): post-strip, any surviving tab is interior scalar content = legal YAML; flagging diverges from real YAML — the exact false-positive class the constraints forbid (Architect).
  - DROP rule 4 (leading indicator char `@`/`` ` ``/`%`): reaches past the verified breakage class, never broke, CI covers it; a 3-of-~18 indicator subset invites drift (5 real cmd files have `allowed-tools` starting `[`) (Pragmatist/Skeptic/Guardian). => ship lean core rules 1+2.
  - Extend existing fixtures (`cmd(quote=...)`, generalize `write_plugin` for agent/skill) rather than fork (Historian).
  - Rule 1 runs FIRST, accepts both `"..."` and `'...'`; helper is a pure @staticmethod (matches `_allowed_tools`). Tests: quoted-`': '` on agent+skill PASS, `[`-leading NOT flagged, CRLF/extra-space robustness (Guardian load-bearing).
[s7] RIGHT-SIZE: NOT split. depth=0 < cap 2. Pragmatist confirmed ONE indivisible change — the fix and any extras hit the same ~5-line `read_frontmatter` seam, share the `cmd()`/`run_on_command()` builders, no internal ordering dep; two PRs against one helper would be worse, nothing independently shippable to peel off.
[s7] REVIEW (Tier 2, pr-review-toolkit:code-reviewer scoped to f5c96ab..HEAD): APPROVE — 0 Critical, 0 Important. Reviewer independently re-ran the suite (16/16), validated the real repo clean, probed _frontmatter_value_error across ~11 edge cases, and scanned every real frontmatter file (exactly 4 values contain ': ', all quoted, none flagged). Confirmed: rule 1 (quote-aware) runs before rule 2; operates on the .strip()ed value (CRLF/trailing-space safe); purely additive inside read_frontmatter (keys dict unchanged -> require_keys / name-matches-dir / check_readonly_no_edit untouched, no regression); stdlib-only, no claude-CLI shellout; tests behavior-focused, not over-mocked. Auto-fix loop NOT entered (nothing at/above the MAJOR bar). Two NITs below the bar, logged not fixed: (1) a malformed quoted-then-trailing value like `"x" then: more` is flagged with the colon-space message though the real defect is bad quoting — documented gap deferred to CI, message still steers toward quoting; (2) an allowed-tools flow token literally containing ': ' (e.g. `["a: b"]`) would be flagged — not a real repo pattern (MCP tools use `__`) and arguably genuinely risky, so flagging is defensible.
[s7] ALIGNMENT PROOF: temporarily re-unquoted peer-review-correctness.md's description (reproducing the f5c96ab bug) -> BOTH gates reject it: local validate_marketplace.py reports the colon-space error AND `claude plugin validate plugins/spec-loop/` reports "YAML frontmatter failed to parse". Restored; git diff shows only the 2 slice files. The local gate now catches the exact class CI catches.
[s7] SIMPLIFY (Step 4b, pr-review-toolkit:code-simplifier, non-blocking): NO edits — code already clear/minimal. Simplifier confirmed the two-guard-clause helper + explicit `str | None` return match the project's guard-clause preference, the long docstring is load-bearing (documents the intentionally-narrow scope + CI deferral), and the new fixtures mirror the existing s5 `write_plugin`/`run_on_command`/`cmd` pattern. Deliberately rejected a dict-dispatch for the `kind`->path chain (lateral; the `else` carries a meaningful pragma guard) and a DRY-extraction of the marketplace scaffolding (would touch the out-of-scope s5 helper or add indirection). Suite still 16/16.
[s7] QUALITY-GATE (Step 4c, config ~/.claude/spec-loop/quality-gate.json): PASS — no refactor needed (0 passes). Measured new/changed code vs thresholds: _frontmatter_value_error lines=30 (<=50; most are the load-bearing docstring, logic ~6 lines), cc=6 (<=10), nesting=1 (<=3), params=2 (<=4); read_frontmatter lines=26 (<=50), cc=6 (<=10), nesting=3 (<=3), params=1 excl. self (<=4); Validator class=265 lines (<=300). CRAP low (both functions simple + fully covered by the new tests). No custom_gates. Thresholds NOT weakened, config NOT edited.

## [run] Gap closure 2/2 — s7 validator hardening — 2026-06-29
- s7 DONE VERIFIED: merge e590fc8 on alpha. scripts/validate_marketplace.py gained _frontmatter_value_error (quote-aware: rule1 returns None for matching "..."/'...'-wrapped values; rule2 flags an UNQUOTED value containing ': '). Council EWC 5/5 — folded: operate on stripped value (CRLF-safe), DROPPED the speculative tab + indicator-char rules (false-positive-prone) for a lean rules-1+2 core. Tier-2 review APPROVE (0 Critical/Important). Quality gate PASS.
- Independent controller verification: test_validate_marketplace 16/16; validate_marketplace.py exit 0; DIRECT heuristic check — FLAGS unquoted colon-space (the original bug), PASSES quoted colon-space, PASSES colon-no-space URL, PASSES plain; sibling suites unaffected (pr_resolver 49/49, dashboard 30/30); `claude plugin validate plugins/spec-loop/` + `claude plugin validate .` both pass (local gate now ALIGNED with the authoritative one). worktree clean. s7 -> complete.
- Local-gate gap CLOSED: the colon-space class that slipped past the regex key-presence parser is now caught locally before push.
- TAG NOTE: v1.1.0-alpha.3 stays at f5c96ab (where plugin content plugins/spec-loop/** was finalized). s7 (e590fc8) touches only scripts/ repo tooling (NOT shipped plugin surface), so the tagged plugin state is unchanged; no tag move needed.
- BOTH process gaps (s6 review parity + local-gate hardening) now CLOSED. All 7 slices terminal.
