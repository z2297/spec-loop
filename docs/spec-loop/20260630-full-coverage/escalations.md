# Escalations — 20260630-full-coverage

## [intake] Define "every line tested": target, client-JS scope, and durability   (status: ANSWERED)
- Trigger: ambiguity + material-assumption (Iron Council ENDORSE_WITH_CONCERNS; skeptic + pragmatist OBJECT on well-posedness)
- Context: "Ensure that every line of code is tested" specifies no metric, target, scope, or enforcement. Baseline is 72.4% line coverage (stdlib trace). Literal 100% forces brittle tests over `__main__`/argparse shims and live-serve/daemon tails; the client JS has a SETTLED prior decision of "no committed JS harness (out-of-tree only)" that a literal reading would reverse.
- The decision: three coupled scope questions (see AskUserQuestion round) — coverage target/definition, client-JS inclusion, durable enforcement.
- Options: see the batched AskUserQuestion round.
- If unanswered: block the run (intake-scoped; nothing scheduled until resolved).
- Answer:
  1. Target = **Coverable-max + documented exclusions**. Cover all meaningful behavior in the importable layer; exclude `__main__`/argparse-glue shims and live-serve/daemon tails via a documented OMIT manifest; ratchet the gate to ~100% of that coverable set. NOT literal 100% of every physical line.
  2. Client JS = **Commit a JS test harness** (human OVERRIDES the prior "no committed JS harness / out-of-tree only" decision). Use Node BUILT-INS ONLY (`node:test`, `node:assert`, `--experimental-test-coverage`) — CI already installs Node 20 — so NO npm/third-party test dependency enters the repo. index.html is in scope.
  3. Enforcement = **Durable**: wire the existing unittest suite into CI (validate.yml), add a stdlib-`trace` Python coverage gate, and a Node JS test+coverage step. Repo-scoped (in validate.yml), deterministic, stdlib/Node-builtin only.

## [s7] Iron Council objects: coverage-fix ordering splits module identity; floor margin too thin for cross-version drift   (status: ANSWERED)
- Trigger: council-objection
- Council verdict: OBJECT (3/5 object — Architect, Skeptic, Guardian; none SAFETY. Pragmatist ENDORSE, Historian ENDORSE_WITH_CONCERNS.)
- Objecting members:
  - Architect — the plan's step-1 ordering ("re-import targets fresh under trace BEFORE running the suite") runs AFTER `main()` already discovered/imported the targets outside the tracer, creating TWO live copies of each target. Measured: baseline 310 tests/0 fail; plan's purge+reimport → 12 errors / 6 failures, suite goes red, gate returns 1 and can NEVER report improved coverage. Remedy proven: move `discover()` INSIDE the traced function, AFTER the fresh re-import, so the suite binds the single traced copy (suite stays green, release.py import lines attributed). This is a seam change to `_load_suite`/`main`/`_run_under_trace`, not "a small helper wired in before runner.run".
  - Skeptic — same module-identity split, plus 54 string-target `mock.patch(...)` sites (18 launcher / 35 pr_resolver / 1 server) resolve through `sys.modules[target]`; after a mid-run swap they patch the NEW object while tests drive the OLD one — threatening the "fail-closed" guarantee (real un-mocked side effects or silently-inert mocks). Also: plan's embedded baseline (188-test suite / TOTAL 54.0%) is stale — worktree HEAD 8cfd864 already has s8+s9 merged, so the tool today runs 310 tests / 79.2%; and no explicit honesty ACCEPTANCE target is defined (floor = "whatever the changed tool emits" is circular).
  - Guardian — floors ratcheted toward ~95%+ "rounded down ~2-3 pts" are baselined on LOCAL python 3.14, but CI runs python 3.12; `co_lines()` executable-line sets can drift across versions (decorators, multi-line calls, match), so a thin 2-3 pt margin can go false-RED and wedge every PR. No 3.12 available locally to validate. (Non-SAFETY: false-red / CI-wedge, not data-loss/security/contract.)
- The decision: how should the slice worker proceed on the objected-to plan?
- Options:
  1. **Revise per the council's converging remedy, then execute (no re-convene)** — (RECOMMENDED DEFAULT) All three objections point at one fixable design flaw with a proven fix: (a) defer test discovery so both the fresh re-import AND `discover()` happen INSIDE `tracer.runfunc`, in that order, giving one traced module identity the suite binds and patches against; (b) add a RED test asserting suite stays green + `testsRun>=MIN_TESTS` AND a known import-time-only line (e.g. a release.py module constant) is now counted, plus an identity assertion (`sys.modules[target]` is the object the discovered test module holds); (c) baseline the ratcheted floors conservatively for py3.12-vs-local drift — widen the round-down margin to >=5 pts (since no 3.12 is available locally) and record this in the baseline comment; (d) refresh the stale 188-test/54.0% baseline block to the current 310-test reality and rewrite the now-false "under-report is the safe direction" docstring (lines 22-31). This keeps the slice's goal, files, and constraints unchanged — only the fix mechanism and floor margin change. The controller may re-dispatch with "apply the council remedy" as the injected answer.
  2. Redirect to the alternative mechanism — instead of re-importing (object swap), trace a fresh import of each target in ISOLATION and UNION its import-time lines into the numerator without swapping the object the suite runs against. Avoids identity risk entirely but is a larger tool change and must prove the unioned lines are the same physical lines the co_lines() denominator counts. Tradeoff: more code, more to verify; option 1 is simpler and already proven green.
  3. Abandon the measurement-honesty fix; keep the current under-reporting tool and only finalize OMIT + docs + ratchet the LOW floors slightly. Tradeoff: leaves the gate dishonest (79.2% reported vs ~95%+ true) — contradicts the run's stated goal of making the gate honest.
- If unanswered: pause this slice (s7); continue all independent slices. s7 runs LAST and depends on the coverage tooling, so pausing it blocks only finalization.
- Answer: **Option 1 — apply the council's proven remedy, then execute (no re-convene).** Defer `discover()` INSIDE `tracer.runfunc` AFTER the fresh target re-import (single traced module identity the suite binds+patches against); add a RED test asserting the suite stays green + `testsRun>=MIN_TESTS` + a known import-time-only line is now counted + `sys.modules[target]` identity holds; widen the ratchet round-down margin to ≥5 pts for py3.12-vs-local `co_lines()` drift; refresh the stale 188-test/54.0% baseline block to the current 310-test/79.2% reality and rewrite the now-false docstring. Goal/files/constraints unchanged.
