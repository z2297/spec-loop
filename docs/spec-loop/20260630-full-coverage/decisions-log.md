# Decisions Log — 20260630-full-coverage

Append-only. One line per autonomous decision made on the human's behalf.

[intake] BASELINE (stdlib trace, 172-test suite): TOTAL 72.4% line coverage (339/1228 missed). release.py 0.0% (127, no test file, PURE — stdlib-only, no subprocess/git), dashboard_server.py 67.0% (124), validate_marketplace.py 69.2% (64), dashboard_launcher.py 92.9% (17), pr_resolver.py 97.5% (7). Client index.html JS: no committed harness.
[intake] COUNCIL: ENDORSE_WITH_CONCERNS — 2/5 OBJECT (skeptic, pragmatist: literal "every line"/100% ill-posed), architect+guardian+historian EWC. No SAFETY. Below majority-halt. Concerns folded; scope ambiguity surfaced to human via escalation-gate (ambiguity + material-assumption triggers).
[intake] CORRECTED FRAMING (architect/pragmatist/guardian): release.py is PURE file-transform (git ops live in release.yml, out of Python scope) — its 0% is a missing test file, not a refactor/hazard; the risk is tests running against real repo root instead of a tmp root. Launcher/server subprocess+HTTP seams ALREADY mocked in existing tests (mock.patch subprocess.run; build_server port=0) — "more tests", not refactors. CI (validate.yml) runs NO python suite today — highest-value gap.
[intake] HUMAN RESOLVED — target=coverable-max+documented-exclusions (not literal 100%); client JS IN scope via a COMMITTED Node-builtin harness (overrides prior out-of-tree-only decision; node:test/node:assert/--experimental-test-coverage, no npm dep); enforcement=durable (wire suite into CI + trace gate + node step). See escalations.md answer.
[intake] DECISION: durable coverage gate lives in the REPO's .github/workflows/validate.yml (repo-scoped, deterministic, stdlib/Node-builtin), NOT in the global ~/.claude/spec-loop/quality-gate.json (which is machine-global across all repos). — RATIONALE: a repo-specific coverage threshold must not leak into every other repo's runs; historian's "reuse quality-gate custom_gates" is satisfied by a single measure_coverage.py tool (no parallel subsystem), scoped where it belongs. — REVERSIBILITY: moderate.
[intake] DECISION (folded council concerns into decomposition): (a) coverage measured with stdlib `trace` only, never coverage.py/pytest [guardian/historian]; (b) new Python tests match scripts/test_*.py conventions — unittest, ephemeral port 0, mock.patch("<mod>.subprocess.run")+argv-recording fakes [historian]; (c) release.py tests MUST run against an isolated tmp root, NEVER the real repo root `.` [guardian INVARIANT]; (d) preserve existing BEHAVIORAL security assertions (loopback publish, :ro, --cap-drop ALL, path-traversal confinement) — never replace with assert-nothing line-hit tests [guardian]; (e) OMIT manifest with documented per-line rationale + per-file floors, not just a global % [architect]; (f) minimal main(argv=None) refactor is the ONLY endorsed testability refactor; everything else additive tests [architect]; (g) JS harness uses Node built-ins only, committed, CI Node 20 [skeptic/historian]. — RATIONALE: iron-council intake feedback. — REVERSIBILITY: moderate.
[s1] COUNCIL on plan: ENDORSE_WITH_CONCERNS — 1/5 OBJECT (guardian, explicitly NOT SAFETY), skeptic+architect+pragmatist+historian EWC. Below majority-halt, no SAFETY → proceed with concerns folded. FOLDED: (a) [guardian, critical] gate must be deterministic on py3.12 (CI) not just py3.14 (local) — pin coverage step to python 3.12 in validate.yml, measure floors under 3.12 OR round down with margin; (b) [guardian/architect] executable_lines() drops lineno is None OR lineno<1 (synthetic co_lines() line 0), unit-tested; (c) [architect] in-process runner asserts suite wasSuccessful() and exits 1 on suite failure; inserts scripts/ on sys.path; normalizes trace absolute paths to relpath keys (scripts/<name>.py), unit-tested; (d) [guardian] harden OMIT against escape-hatch drift — every entry requires a rationale, tool errors on malformed/rationale-less lines; (e) [historian] pin CI test step to exact `python3 -m unittest discover -s scripts -p 'test_*.py'`; key floors by full relpath; reconcile step-5 measurement against recorded baseline (72.4% total); (f) [skeptic] gate must pass on the SAME commit that sets floors (self-consistency). OVERRULED: [pragmatist] drop per-file floors — kept, mandated by prior decision 10(e)/shared_constraint #1 (a well-covered file must not mask a bare one). — REVERSIBILITY: high.
[s1] BASELINE RECONCILED (historian concern): measure_coverage.py's REAL run (188-test suite, py3.14 local) = TOTAL 54.0% (655/1213); per-file launcher 65.7% (157/239), server 45.0% (166/369), pr_resolver 80.7% (221/274), release 0.0% (0/125), validate_marketplace 53.9% (111/206). This DIFFERS from the intake-recorded 72.4% because co_lines() yields a LARGER, more honest executable denominator (counts every bytecode-landable line incl. multi-line statement continuations) than the intake ad-hoc measurement. The tool's own denominator is what CI enforces, so floors are set from IT, rounded DOWN ~2-3pts for py3.12 margin: launcher 63, server 42, pr_resolver 78, release 0, validate_marketplace 51, TOTAL 51. Not a regression — same suite, different (better) measurement basis; later slices raise coverage, final slice ratchets floors. — REVERSIBILITY: high (floor integers).
[s1] REVIEW (Tier-2: code-reviewer + silent-failure-hunter): 0 critical/major from code-reviewer; silent-failure-hunter raised 2 CRITICAL false-GREEN vectors (at/above bar) — FIXED in 1 auto-fix pass: (C1) empty/collapsed test discovery makes wasSuccessful()==True over 0 tests → added MIN_TESTS=150 guard + prints "suite: N tests passed"; main() now refuses to measure if tests_run<MIN_TESTS (verified rc=1 on empty suite). (C2) an overshooting OMIT range could collapse a file to 0/0=100% → added validate_omit(): rejects OMIT lines past end-of-file and caps OMIT at MAX_OMIT_FRACTION=25% of a file's executable lines (verified raises on overshoot). Also folded: exception chain (raise ... from exc), removed unused `import os`, dropped unused main(argv) param, and documented runtime-coverage semantics (import-time lines counted executable-but-unexecuted — safe false-red direction, self-consistent floors) [code-reviewer item1 / hunter H1]. H2/H3 (regress-to-floor alarm, new-script auto-detection) noted as future-slice hardening, below bar for s1 (release.py:0 is the documented ratchet start). Gate PASSES green post-fix (193 tests; TOTAL 54.0%). — REVERSIBILITY: high.
[s1] SIMPLIFY (code-simplifier, non-blocking): DRY'd the `100.0 if able==0 else 100.0*run/able` divide-by-zero idiom into a single source (FileStat.pct) — evaluate() and _print_report() TOTAL row now build a FileStat and reuse .pct; fixed stranded `import sys` ordering in test file. No behavior change; 21 tests + gate re-verified green.
[s1] QUALITY-GATE: initial FAIL (2 breaches in measure_coverage.py) → PASS after 1 behavior-preserving refactor pass. (1) parse_omit cyclomatic complexity 13 > 10 → extracted _parse_line_range() + _parse_omit_line() helpers; parse_omit now a thin loop (cc well under 10). (2) validate_omit parameter_count 5 > 4 → grouped (relpath, executable, line_count) into a FileLines dataclass and read MAX_OMIT_FRACTION as the module constant (params 5→2). All metrics within thresholds (cc<=10, params<=4, method_lines<=50, nesting<=3, class_lines<=300). No behavior/signature-observable/test-expectation change beyond the internal refactor; 21 unit tests + coverage gate re-verified green. — REVERSIBILITY: high.
[s1] MERGE: slice merged into alpha via --no-ff (merge commit f5c84ef). Post-merge suite (193 tests) + coverage gate re-verified green on alpha. PUSH to origin/alpha NOT completed — environment lacks git push credentials (HTTPS password auth unsupported, no gh CLI); consistent with prior slices integrated locally. Non-blocking: controller/human to push. — REVERSIBILITY: high.
[s3] COUNCIL on plan: ENDORSE_WITH_CONCERNS — 5/5 EWC, 0 OBJECT, 0 SAFETY → proceed with concerns folded. FOLDED: (a) [guardian] main(argv=None) uses `argv = sys.argv[1:] if argv is None else argv` + `argv[0] if argv else "."` — NO raw sys.argv[1] index; keep not-a-directory/OK:/FAILED: strings + both exit codes byte-identical; touch ONLY main(), no validate_* method; __main__ guard stays sys.exit(main()); (b) [guardian/historian INVARIANT] MainCliTest default-arg case chdir's into a fresh TemporaryDirectory (restore via addCleanup) and asserts main([]) returns an int without raising — never runs against live checkout, never asserts a fixed exit code; all other main() cases pass an explicit tmp root via main([str(root)]); (c) [historian] capture via contextlib.redirect_stdout/redirect_stderr(io.StringIO()) verbatim per test_dashboard_launcher.py; main(argv=None) mirrors pr_resolver.py:459/dashboard_launcher.py:536 precedent (also intake decision (f)); RealRepoTest left untouched as the behavior-preservation guard; (d) [architect] add to Task 6 a MALFORMED plugin.json case (covers validate_plugin line 155 + load_json invalid-JSON arm) and a command OMITTING allowed-tools (covers _allowed_tools line 226 empty-set arm); Task 3 pluginRoot case uses a BARE-name source ("demo" not "./demo") so line 113 is actually reached; (e) [pragmatist] no bespoke _rel private-helper test — cover the ValueError fallback incidentally via an observable error routing a foreign path, or drop if already hit; treat Tasks 3-6 as a value-ordered menu of currently-missed arms, skip redundant assertions on branches a neighbor already covers; (f) [skeptic] concrete done-condition = measure_coverage PASS + validate_marketplace.py ≥ 51% floor + no other file regresses; corrected the plan's docstring Note (measure_coverage uses co_lines(), does NOT count docstrings — uncoverable lines are import-time headers/constants/__main__ guard, ~34; ~63 reachable). Architect measured target ~84.5% (174/206) — floor cleared with wide margin. — REVERSIBILITY: high.
[s3] NOTE FOR s7 (coverage-lock seam): s3 raises validate_marketplace.py coverage from 53.9% toward ~84.5% but does NOT ratchet its 51% floor — floor ratcheting is s7's job. No measure_coverage.py / coverage_omit.txt / validate.yml edits made in s3. No new OMIT exclusions needed (all uncovered lines are either now test-covered or are import-time-only lines the tool already treats correctly). — REVERSIBILITY: high.
[s3] REVIEW (Tier-2: code-reviewer + pr-test-analyzer): PASS, ZERO P0/P1 (blocking) findings. code-reviewer verified the main(argv=None) refactor is behavior-preserving (equivalent for all inputs; exit codes/OK:/FAILED:/error: messages + validate_* logic untouched; gate not weakened), tests use isolated tmp fixtures asserting observable behavior, RealRepoTest still guards the checkout. pr-test-analyzer mapped all 24 self.err branches to real inputs (24/24 reached), confirmed path-traversal/object-source-SHA-ref/malformed-JSON/missing+unterminated-frontmatter arms covered and the positive object-source test reaches validate_source_object non-vacuously. BELOW-BAR (logged, non-blocking): (P2, criticality 4/10) only github object-source kind exercised — FOLDED: added test_git_subdir_missing_one_of_multiple_keys_fails to drive the multi-key required-key loop; (P2 2/10) _rel ValueError fallback likely unreachable in practice (traversal return fires first) — left dark per pragmatist; (P2 3/10) pluginRoot-without-string-pluginRoot arm — trivial guard already hit by no-metadata fixtures, skipped; (P3) os.chdir global-state seam in default-arg test — acceptable under serial unittest runner, guarded by addCleanup LIFO restore; (P3) inline __import__("shutil") style outlier — FOLDED: hoisted to top-level `import shutil`. Post-fix: 49 file tests / 226 suite green, validate_marketplace.py 83.2%, gate PASS. — REVERSIBILITY: high.
[s3] SIMPLIFY (code-simplifier, non-blocking): one minimal readability edit — renamed single-letter local `m` -> `marketplace` in single_entry_marketplace to match the file's descriptive-local convention. Explicitly declined churn: kept the idiomatic main(argv=None) pattern, kept the file's inlined `assertTrue(any(... in e ...))` assertion convention (a shared helper would diverge from pre-existing test classes). No behavior/signature/assertion change; run_on_root tmp-isolation invariant intact. 49 tests green. — REVERSIBILITY: high.
[s3] QUALITY-GATE: PASS on first measurement, no refactor loop needed. All changed code within configured thresholds (cc<=10, cognitive<=15, method_lines<=50, params<=4, nesting<=3, class_lines<=300): validate_marketplace.py main(argv=None) refactor is trivial (cc 2); all new test fixture builders + TestCase methods (MainCliTest/MarketplaceStructureTest/PluginEntryTest/SourceObjectTest/PluginManifestTest) are flat single-assertion helpers well under every bar; no test class exceeds class_lines. No threshold weakening; quality-gate.json untouched. — REVERSIBILITY: high.
[s3] MERGE: slice merged into alpha via --no-ff (merge commit 97473c6) from base f5c84ef; branch spec-loop/20260630-full-coverage/s3 (fd95f77..153dcf1). Clean merge — no file overlap with sibling s2's advance (s2 touched release.py/test_release.py/test_dashboard_launcher.py; s3 touched only validate_marketplace.py + its test + s3 plan doc). Post-merge full suite (255 tests) + coverage gate re-verified green on alpha: validate_marketplace.py 83.2% (173/208, floor 51), all per-file + TOTAL (68.8%) floors met, no regression. PUSH to origin/alpha NOT completed — environment lacks git push credentials (consistent with s1/s2); controller/human to push. — REVERSIBILITY: high.
[s2] COUNCIL on plan: ENDORSE_WITH_CONCERNS — 5/5 EWC, 0 OBJECT, 0 SAFETY → proceed with concerns folded. FOLDED: (a) [skeptic/guardian] date determinism — assert the rolled CHANGELOG heading via datetime.date.today().isoformat() computed at test time (or a "## [<ver>] - " prefix + date-format regex), never a literal date string, so Step 4/6/7 don't flake across a day boundary; (b) [pragmatist] trim Step 7 stable happy-path — cut_release (Step 6) already pins all three content effects, so main() happy-path asserts return 0 + one smoke check (plugin.json bumped), no duplicate full-effect verification; (c) [architect] fixture fidelity — seed_repo's marketplace.json seeds BOTH a channel entry (name NOT matching ^spec-loop-\d) AND >=1 existing archive entry (name matching ^spec-loop-\d) so add_archive_entry's insert_at lands BETWEEN them (else the ordering test passes vacuously); a seed_changelog(with_unreleased=) knob covers roll_changelog's rewrite path AND its n==0 WARNING no-op path; the missing-marketplace main() case seeds a root WITHOUT .claude-plugin/marketplace.json (not a malformed one) to hit release.py:169-171 cleanly; (d) [guardian] self-enforcing isolation — the tmp-root helper asserts the resolved root is under tempfile.gettempdir() so a mis-authored test fails loudly instead of silently mutating the live checkout; --notes/error-return cases re-read the seeded fixtures and assert content UNCHANGED (mutates nothing), not just the return code; (e) [historian] use the repo's contextlib.redirect_stdout/redirect_stderr(io.StringIO()) capture idiom (per test_dashboard_launcher.py) for the --notes-output and beta-suffix-warning assertions, and always call release.main(["<ver>", ..., "--root", str(tmp)]) passing --root <tmp> even on validation/error paths so Step 7 honors the isolated-tmp-root invariant too. Verified [historian]: main(argv=None) matches pr_resolver.py:459/dashboard_launcher.py:536 precedent and is pre-endorsed by intake decision (f); [guardian] only caller of release.main is the CLI subprocess in release.yml, so the refactor is behavior-preserving. Also noted [skeptic]: the sibling RealRepoTest regression guard is DELIBERATELY omitted here — release.py mutates in place and constraint (c) forbids touching the real repo. — REVERSIBILITY: high.
[s5] COUNCIL on plan: ENDORSE_WITH_CONCERNS — 5/5 EWC, 0 OBJECT, 0 SAFETY → proceed with concerns folded. Right-sizing [pragmatist]: exactly ONE independently shippable change (one file, one coverage goal, clean deps:[s1]) — NO split. FOLDED: (a) [pragmatist/architect/skeptic] TRIM Step A to the single new line _port_bound line 358 ("bind for") — the other disjuncts + _name_conflict True/False are ALREADY green via existing main-level tests (test_port_bound_gives_actionable_message hits 357, test_name_conflict_reuses hits 350); TRIM Step B to the one genuinely-uncovered branch _handle_run_failure 400-401 (generic "docker run failed"), drop the "for completeness" re-assertion of name-conflict/port-bound return paths (already exercised end-to-end through main); (b) [guardian] on the empty-argvs (418) and FALLBACK (507) fallback sub-cases, ADD a positive assertion that the recording subprocess.run fake recorded ZERO ["docker","run",...] argvs — turns the "never invokes docker" invariant into a test, mirroring existing TestMainLaunch assertions; (c) [historian/architect] use mock.patch.object(dl, "_fallback", sentinel) (the file's line-512 module-attr convention) to neutralize the os.execvp process-replacement hazard at 418/507 — the surgical, correct seam; keep it (do NOT switch to patching only subprocess.run, which would let execvp fire); (d) [architect] Step B generic-branch stderr must match NEITHER _name_conflict (checked first, line 393) NOR _port_bound — use a plain "some other error"; (e) [guardian] Step D reuses TestRegistry's tempfile.TemporaryDirectory setUp/tearDown lifecycle (never writes to real ~/.spec-loop/dashboard) and the corrupt-payload case asserts the "unreadable" stderr warning; (f) [skeptic] restate success criterion as BEHAVIORAL — all coverable runtime-body branches (358, 400-401, 418, 422-424, 446-451, 502-503, 507) executed; only structurally-untraceable import/def-header/module-const lines and the OMITted 551-552 shim remain un-hit — not "above 63% floor" (already met at 65.7%). Append-only test additions; all existing security-invariant assertions untouched. — REVERSIBILITY: high.
[s5] NOTE for s7 (OMIT/floor ratchet): scripts/dashboard_launcher.py:551-552 (__main__ shim) is genuinely unreachable under the trace gate; per skeptic it is ALREADY present in scripts/coverage_omit.txt (added in s1) — no new OMIT entry needed. s5 does NOT edit coverage_omit.txt/measure_coverage.py/validate.yml. After s5 raises launcher runtime coverage, s7 may ratchet the launcher floor above 63%.
[s4] COUNCIL on plan: ENDORSE_WITH_CONCERNS — 5/5 EWC, 0 OBJECT, 0 SAFETY → proceed with concerns folded. ROOT-CAUSE (empirically confirmed by skeptic+architect): dashboard_server.py reads 45% NOT because the handler is untested but because measure_coverage's trace.Trace(count=1) traces only the CALLING thread; the existing HTTP tests exercise the handler over a real socket on a daemon serve_forever thread → the whole request/response path (do_GET/_route/_serve_static/_read_asset/_emit/_content_type/_json_response) is invisible to the tracer though behaviorally exercised. Lever: invoke the handler methods + helpers SYNCHRONOUSLY on the test thread (additive, tracer-visible), keep every real-socket security test untouched. FOLDED: (a) [skeptic/architect] the serve_forever tail is ALREADY OMITted in coverage_omit.txt (dashboard_server.py:654-659) as is the __main__ shim (:663-664); the main(argv=None) edits (lines 624+638, no line-count change) do NOT shift them — so s4 needs NO OMIT change and NO s7 OMIT handoff; only VERIFY the range and log the per-thread-trace finding; (b) [pragmatist/guardian] Task 2 uses ONE seam — handler=Bound.__new__(Bound); set headers/path; wfile=io.BytesIO(); let _emit run FOR REAL and assert on wfile.getvalue() bytes — NO send_*/end_headers call-spies (spy asserts test implementation); this yields real X-Content-Type-Options:nosniff/Content-Length/conditional-ETag assertions (nosniff asserted by NO socket test today — assurance gain); (c) [guardian INVARIANT] NEVER stub the security primitives — in-thread 421/traversal tests run the REAL _host_allowed (real allowed_hosts from build_server) + REAL resolve_within/os.path.isfile; include the absent-Host (headers.get("Host") is None) branch; (d) [guardian] 405 is UNREACHABLE via _route (lives in _reject_method, wired do_POST=...=_reject_method) — do NOT "cover" 405 in-thread; test_non_get_method_returns_405 stays its sole source of truth; (e) [pragmatist] _serve_api_runs/_serve_api_run_detail/_collection_material in-thread tests are THIN tracer-visibility mirrors (one call + status/decodable-body), NOT a second security/multi-root suite; (f) [architect/historian] build the bound handler via ds.build_server(...).RequestHandlerClass (single wiring authority), never a parallel type(...); (g) [guardian/historian] main() test: mock.patch("dashboard_server.ThreadingHTTPServer") (mirrors mock.patch("dashboard_launcher.subprocess.run")); mock exposes .server_address; raise KeyboardInterrupt from serve_forever to cover the except/finally:server_close() branch (behavioral only — OMITted lines); MANDATORY --port 0 in every main() invocation (never bind DEFAULT_PORT 8787); capture the nonexistent-root warning via redirect_stderr(io.StringIO()) and assert "does not exist" text (s2 precedent); keep -> int (matches pr_resolver.py:459). — REVERSIBILITY: high.
[s4] PER-THREAD-TRACE FINDING (for s7 / future coverage work): measure_coverage.py's trace.Trace(count=1) records only the thread it is invoked on. Any code exercised solely on a background/daemon thread (e.g. serve_forever request handling) is behaviorally correct yet reads as UNCOVERED. To raise MEASURED coverage of thread-dispatched handler code, exercise it synchronously on the test thread. No coverage_omit.txt / measure_coverage.py / validate.yml edits made in s4; the serve_forever tail + __main__ shim are already OMITted and the main(argv=None) refactor does not shift those ranges. After s4 raises server runtime coverage, s7 may ratchet the dashboard_server.py floor above 42%.
[s4] MERGE: slice merged into alpha via --no-ff (merge commit 77d8db5). Branch spec-loop/20260630-full-coverage/s4 (commits 700b429..70e34ad). Post-merge full discovered suite (219 tests) + coverage gate re-verified green on alpha; dashboard_server.py 72.1% (266/369) vs 42% floor; TOTAL 77.0% (composed with other merged slices). PUSH to origin/alpha NOT completed — environment lacks git push credentials (consistent with s1/s2/s3/s5); controller/human to push. — REVERSIBILITY: high.
[s4] QUALITY-GATE: PASS, 0 refactor passes. No real analyzer installed (radon/lizard/flake8 absent; skill forbids installing) → heuristic AST measurement of the slice's CHANGED code only, marked heuristic. main() (only changed production fn): cc=6 (≤10), nesting=2 (≤3), params=1 (≤4), lines=37 (≤50) — the 4-line argv=None/parse_args(argv) edit changed no metric. New test fns: max cc=5, nesting=3, params=4, lines=48 — all within thresholds (cyclomatic≤10, cognitive≤15, method_lines≤50, params≤4, nesting≤3, class_lines≤300). CRAP skipped (no CRAP-mapping coverage tool present) — noted, not fabricated. No threshold/config edit. — REVERSIBILITY: n/a (measurement).
[s4] SIMPLIFY (code-simplifier, non-blocking): two behavior-preserving clarity edits to the NEW test code only (no security primitive stubbed, no assertion weakened, pre-existing socket suites untouched): (1) dropped make_handler's always-default roots/assets_subdir params (every call was make_handler(self.tmp)) + the dead `if not assets.exists()` guard (fresh TemporaryDirectory each time); (2) extracted a _mocked_serve() @contextmanager to de-triplicate the ThreadingHTTPServer-mock + redirect_stdout/stderr boilerplate across the three MainEntrypointTests. 73 tests + gate re-verified green, server steady at 72.1%. — REVERSIBILITY: high.
[s4] REVIEW (Tier-3: code-reviewer + silent-failure-hunter + pr-test-analyzer + type-design-analyzer, all parallel): 0 P0, 0 P1 — nothing at/above the P0,P1 blocking bar; no auto-fix loop needed. code-reviewer + pr-test-analyzer independently reproduced the coverage lift (server 72.1%, 266/369) and adversarially confirmed the security tests are LOAD-BEARING (monkeypatching a naive resolve_within makes the traversal test return 200 + "TOP SECRET" and fail — the guarantee is genuinely pinned; the socket suites remain untouched as source of truth). type-design: argv=None is textbook-idiomatic. Below-bar P2s folded voluntarily (cheap, test-strengthening, non-behavioral): (1) [test-analyzer] the vanished-run "-" cache-key else-branch (dashboard_server.py:442) was never actually taken (fixture never deleted a run) → split into test_collection_material_ok + test_collection_material_weak_key_when_run_unresolvable, the latter calling _collection_material([{"run_id":"vanished"}]) directly to assert the real "vanished:-" weak key; (2) [silent-failure-hunter/test-analyzer] test_main_default_root_is_cwd asserted only rc==0 → now spies the real build_server to assert the default "." root resolved to the tmp cwd (realpath-equal) and "serving 1 root(s)" printed. P2s NOT folded (deliberate): 405 stays socket-only (Guardian: unreachable via _route, lives in _reject_method); MainEntrypointTests docstring is accurate. All 73 file tests + gate re-verified green post-fold. — REVERSIBILITY: high.
[s2] REVIEW (Tier-2: code-reviewer + pr-test-analyzer): 0 CRITICAL/MAJOR/MINOR from either — nothing at/above the HIGH/MAJOR bar, no auto-fix loop needed. code-reviewer VERIFIED: main(argv=None)/parse_args(argv) behavior-preserving (only callers = __main__ shim sys.exit(main()) and the release.yml CI subprocess, both invoke main() with no args → argv=None → parse_args reads sys.argv identically); GUARDIAN INVARIANT satisfied (every test uses make_root() which asserts the root resolves under tempfile.gettempdir(), every rel.main([...]) passes --root str(root) incl. error paths, no reliance on default --root "."); tests assert file CONTENT not line-hits; stdlib conventions match siblings. FOLDED (below-bar but cheap/correct per receiving-code-review): (1) [test-analyzer crit-6] the add_archive_entry insert_at=len(plugins) fallback (first-ever archive, no pre-existing spec-loop-\d entry) was untested and the with_archives=False fixture param was defined-but-unused → added test_first_archive_appends_after_channels seeding with_archives=False, asserting the new entry lands after spec-loop-alpha (19 tests now). DEFERRED (below bar, marginal): (2) [test-analyzer crit-5] clean beta-through-main-with-suffix path (rc 0, no warning) — the warning branch is covered by test_beta_without_suffix_warns_but_bumps and cut_release's beta path by test_prerelease_bumps_only; a main() wrapper over already-covered logic is not worth a dedicated case. NIT (cosmetic, no change): make_root's temp self-check runs on td.name not the with-bound root (same path). — REVERSIBILITY: high.
[s2] COVERAGE (for s7): release.py 0.0% (0/125) -> 82.4% (103/125), well above its 0% floor; TOTAL 54.0% -> 62.5% (758/1213); suite 193 -> 212 tests, gate PASS. The 22 uncovered executable lines are ALL import-time (docstring, imports, module constants SEMVER/PLUGIN_MANIFEST/etc, and def-header lines — counted executable but attributed at import, before tracing, the documented under-report bias) plus line 194 (__main__ shim, already OMITted). ZERO genuinely-untested logic lines — every function body and every main() branch is exercised. NO new coverage_omit.txt entries are warranted for s7. — REVERSIBILITY: n/a (measurement).
[s6] COUNCIL on plan: 1/5 OBJECT (architect), 4/5 ENDORSE_WITH_CONCERNS (skeptic, pragmatist, guardian, historian). Below majority-halt; NO SAFETY (guardian explicit) → proceed with concerns folded, NOT escalated. ARCHITECT OBJECT (technical blocker, resolved in-plan not escalated): node:vm string-load yields ZERO --experimental-test-coverage attribution for index.html (empirically verified by architect AND re-verified by me). RESOLUTION: transport changed from node:vm to REAL-MODULE import — harness extracts inline <script>, strips UMD tail, appends ESM export, writes a temp .mjs under os.tmpdir(), await import()s it; V8 then instruments it and attributes coverage (verified: extracted module shows in coverage table). Keeps slice to its 3 declared files (no committed sibling module the browser must fetch) AND keeps page self-contained. FOLDED: (a) [architect/guardian/pragmatist] extend HAS_DOM guard to line 163 `let view = parseHash()` (Node-safe default {kind:"overview"}) — omitted in first draft, crashes under Node otherwise; (b) [architect] DOM shim append(...children) variadic handling element+text nodes; drop renderOverview from tested set (builders cover render logic); (c) [skeptic] coverage success = named functions covered by passing tests, NO numeric floor on Node 20 (per-metric flags are Node 22+), numeric gate deferred to s7 — recorded; enumerate in-scope funcs + declare poll/fetch/freshness/orchestrator layer OUT of scope; add browser-parity done-criterion; (d) [pragmatist] trim export surface to exactly the tested funcs (labelClass, sha7, truncate, groupRunsByRoot, parseHashFrom, el, overviewCard, rootGroupSection, sliceRow, __setDocument); (e) [guardian] negative XSS assertion (rootGroupSection <h3> zero child element nodes on markup root key); pin validate.yml insertion point (immediately after setup-node@v4, before Install Claude Code CLI); target explicit test file; assert no fetch/bootstrap on load; (f) [historian] fail-closed on zero discovered tests via explicit-file target; header docstring mirrors test_dashboard_server.py intent; s7 JS gate reuses Python durable-gate philosophy. RIGHT-SIZING [pragmatist]: correctly ONE independently-shippable slice (seams+test+CI step mutually dependent) — NOT split. — REVERSIBILITY: high.
[s2] SIMPLIFY (code-simplifier, non-blocking): DRY'd the 19 tests' repeated `with make_root() as td: root = Path(td); seed_repo(root, ...)` boilerplate into a single @contextlib.contextmanager helper seeded_root(**seed_kwargs) yielding the Path (mirrors test_validate_marketplace.py's run_on_command wrapper), ~40 lines removed. Guardian invariant intact (seeded_root routes through make_root, whose OS-temp-dir assertion still runs per test). No behavior/name/assertion changes; release.py main() diff left as-is (already minimal). 19 tests + coverage gate re-verified green (release.py 82.4% unchanged). — REVERSIBILITY: high.
[s5] REVIEW (Tier-2: code-reviewer + pr-test-analyzer): PASS — 0 P0/P1 from both. code-reviewer confirmed every hard constraint honored (subprocess.run fully mocked so _run never runs unmocked; os.execvp neutralized via mock.patch.object(dl,"_fallback",...); real docker never invoked; read/write_mount_set confined to a tempdir, never ~/.spec-loop; append-only diff leaves security-invariant assertions untouched). pr-test-analyzer independently confirmed via sys.settrace that all 16 targeted branch lines (358, 400-401, 418, 422-424, 446-451, 502-503, 506-507) genuinely execute and assert observable outcomes (rc, stderr/stdout text, no-docker-run invariant), not implementation. Below-bar notes (recorded, NOT fixed): P3 unused `decision` arg in the _execute_plan abort test (harmless, documents intent); P3 _fallback assert_called_once is the pragmatic execvp seam (behavioral rc/no-docker asserts remain primary); P2 benign "No such container" teardown-continue path is already covered by pre-existing test_recreate_tolerates_no_such_container_on_teardown (jointly covers the 421 branch pair) — no duplicate added. — REVERSIBILITY: high.
[s5] SIMPLIFY (code-simplifier, non-blocking): NO CHANGE — the s5 additions already meet the clarity/DRY bar and match the file's per-test inline recording-fake convention (every pre-s5 TestMainLaunch test defines its own inline `run` closure); extracting a shared helper would reduce consistency and touch the pattern asked to be preserved. All 70 file tests pass.
[s5] QUALITY-GATE: PASS (no refactor pass needed). All s5 test methods within thresholds — max cyclomatic 5 (<=10), max method 22 lines (<=50), max nesting 2 (<=3), params 1 (<=4), max class 44 lines (<=300). Tests-only change; no production complexity introduced. — REVERSIBILITY: high.
[s5] MERGE: slice merged into alpha via --no-ff (merge commit ccce2bb; slice commit c54c35d). Post-merge full suite (203 tests) + coverage gate re-verified green on alpha; dashboard_launcher.py 65.7%->72.4% (157->173/239, +16 lines), TOTAL 54.0%->55.3%. Worktree removed + branch deleted. PUSH to origin/alpha NOT completed — environment lacks push credentials (consistent with prior slices); controller/human to push. — REVERSIBILITY: high.
[s2] QUALITY-GATE: initial FAIL (1 breach) → PASS after 1 behavior-preserving refactor pass (of 3 budget). Breach: seed_repo had parameter_count 5 > 4 (root + 4 keyword flags plugin_version/with_marketplace/with_archives/with_unreleased). Fix: grouped the 4 fixture-shape flags into a frozen RepoSpec dataclass (defaults = realistic repo) so seed_repo(root, spec) has 2 params; seeded_root(**spec_kwargs) builds RepoSpec(**spec_kwargs), so no test call site changed (they still read seeded_root(with_archives=False) etc — same keyword names as dataclass fields). No behavior/observable-signature/test-expectation change beyond the internal fixture-builder refactor; matches the s1 FileLines-dataclass precedent for the same param-count breach. All other functions/classes in both changed files within thresholds (cc<=10, method_lines<=50, params<=4, nesting<=3, class_lines<=300). 19 tests + coverage gate re-verified green (release.py 82.4%). — REVERSIBILITY: high.
[s2] MERGE: slice merged into alpha via --no-ff (merge commit fb1a744; slice commit a576ce0, cut from f5c84ef). Sibling slice s5 (ccce2bb, dashboard_launcher coverage) had merged into alpha meanwhile; git merged s2 cleanly on top (disjoint files) — f5c84ef confirmed ancestor, no divergence/lost work. Post-merge on alpha: full suite 222 tests OK (s2's 19 + s5's), coverage gate PASS green — release.py 82.4% (103/125), dashboard_launcher 72.4% (s5), TOTAL 63.8% (774/1213). Worktree removed, slice branch deleted. PUSH to origin/alpha NOT completed — environment lacks git push credentials (same as prior slices, integrated locally); controller/human to push. — REVERSIBILITY: high.
[s6] REVIEW (Tier-2: code-reviewer + silent-failure-hunter + pr-test-analyzer): code-reviewer 0 blocking (2 P2). Round-1 P1s FIXED in auto-fix pass 1: (P1-a hunter) the "no bootstrap" test was a tautology (X===X, assert.ok(true)) AND import happened once at top-level — replaced with a real dormancy test that spies globalThis.fetch/setInterval/setTimeout/window.addEventListener and imports a FRESH temp-module copy under them, asserting zero calls (verified: an ungated top-level fetch()/setInterval() trips the counters → deepEqual fails); (P1-b hunter) CI step was fail-OPEN on an emptied suite (node --test exits 0 on zero tests) — added a TAP `# tests N` count floor (>=12), mirroring Python MIN_TESTS; (P1-c test-analyzer) added overviewCard unreadable→transient branch test; (P1-d test-analyzer) added labelClass prototype-key hardening test (constructor/__proto__/toString/hasOwnProperty/valueOf → lbl-unknown). Folded cheap P2s: bidirectional export drift-guard (exact-set match of module.exports object-literal identifiers vs EXPORTS), mixed/non-string-root grouping, sliceRow tier/depth/deps cell asserts, truncate/sha7 exact-length boundaries. Round-2 hunter found the auto-fix INTRODUCED a new P1: the CI step captured node output into a command-substitution and gated only on count, DISCARDING node's exit status (pipefail inert, no pipe) → a ≥12-test suite with a failing test would exit 0. FIXED in auto-fix pass 2: capture rc=$? immediately after the substitution + `exit $rc` on nonzero, in addition to the count floor (verified 3 modes: pass→pass, one failing test→fail rc, empty→fail count). Round-3 confirmation: slice CLEAR at Tier-2 bar, no residual P0/P1; 1 remaining P2 (dormancy deepEqual is belt-and-suspenders since an ungated bootstrap also throws first — fails closed either way, advisory). 15 tests pass; coverage attributes to the real extracted client module (~49% line — the deliberate in-scope subset; poll/fetch/freshness/orchestrator layer excluded per s6 boundary). — REVERSIBILITY: high.
[s6] SIMPLIFY (code-simplifier, non-blocking): removed the duplicate `elementsOf` free-function in index.test.mjs and routed all element-child queries through the DOM shim's own `.children` getter (identical nodeType===1 filter), matching the XSS-invariant test's accessor. index.html + validate.yml left untouched (guards/seams/CI two-gate logic are minimal and constraint-bound). No behavior/assertion change; 15/15 tests pass.
[s6] QUALITY-GATE: PASS (heuristic — no JS metric tool installed; eslint/eslintcc/lizard/radon absent and offline install disallowed, so measured per refactor-analysis checklists on the slice's changed functions). Thresholds cc<=10, cognitive<=15, method_lines<=50, params<=4, nesting<=3, class_lines<=300. index.html changed fns: __setDocument (cc1/1 line/1 param/nest0), parseHashFrom (cc3/~7 lines/1 param/nest2), parseHash (cc1/1 line), el (1 line changed, shape unchanged) — all PASS. index.test.mjs fns: extractClientSource (cc1, no branches, ~16 exec lines, 0 params, nest1), loadClientModule (cc1/4 lines/1 param), makeDom/makeNode (per-method cc<=2, nest<=1), dormancy test cb (cc~3 with one try/finally + one equality check, ~20 exec lines, 0 params, nest<=2) — all PASS. CRAP: SKIPPED (no tool computes per-method CRAP; JS coverage exists but not fabricating a CRAP number). class_lines: N/A — index.test.mjs is a flat suite of small independent test() units, not a cohesive class/god-object; module-as-test-file is within intent. No refactor passes needed (0/3). — REVERSIBILITY: high.
[s6] MERGE: slice merged into alpha via --no-ff (merge commit d449ae7). Merged current alpha (77d8db5, which had advanced with sibling slices s2/s3/s4 — no file overlap with s6's 3 files) into the slice branch first, re-verified, then integrated. Post-merge on alpha: Node client JS suite 15/15 pass; Python unittest 281 tests OK; coverage floor gate PASS (TOTAL now 77.0%, raised by siblings); marketplace validation OK. — REVERSIBILITY: high.
[s6] PUSH to origin/alpha NOT completed — environment lacks git push credentials (HTTPS "could not read Username", no gh CLI); consistent with prior slices integrated locally. Non-blocking: controller/human to push. — REVERSIBILITY: high.
[s5] DONE — dashboard_launcher.py 65.7%->72.4% (real-logic ~99.6%); merge ccce2bb.
[s2] DONE — release.py 0.0%->82.4%; merge fb1a744.
[s3] DONE — validate_marketplace.py 53.9%->83.2%; merge 97473c6; minimal main(argv=None).
[s4] DONE — dashboard_server.py 45.0%->72.1%; merge 77d8db5; Tier-3 4-reviewer ship-it; security behavioral asserts intact; minimal main(argv=None).
[s6] DONE — committed Node-builtin JS harness (node:test, --experimental-test-coverage) for index.html via dual-mode inert seams; CI node step added; merge d449ae7.
[wave2] INTEGRATION CHECK: 281 tests pass, coverage gate PASS, TOTAL(production tool) 54.0%->77.0%. Cleaned lingering s4/s6 merged worktrees+branches.
[wave2] FINDING (autonomous, proceed-and-log): (A) the production measure_coverage.py under-reports because tracing starts AFTER module import — import-time lines (module constants, def/decorator headers, docstrings) count as executable-but-unattributed; true logic coverage is ~95%+ (import-in-trace re-measure: server 94.9%, launcher 99.6%, pr_resolver 97.5%, validate_marketplace 98.6%). (B) pr_resolver.py received NO coverage slice (intake ad-hoc 97.5% masked it under the honest denominator) and has 6 genuinely-uncovered logic lines (malformed-URL/HTTP-error branches). (C) residual coverable branches remain in dashboard_server (~15) and validate_marketplace (2). The 4 `sys.exit(main())` __main__ shims are legit OMIT.
[wave2] DECISION: graft s8 (close pr_resolver + validate_marketplace logic gaps) and s9 (close dashboard_server logic gaps) as depth-0 pending slices deps=[s1]; EXPAND s7 to also FIX measure_coverage.py import-attribution so reported coverage reflects true logic coverage, and make s7 depend on s8+s9 so it ratchets to the final honest coverable-max. — RATIONALE: human target = coverable-max of all meaningful behavior; error/dispatch/defensive branches are meaningful (guardian: test behavior); closing them IS the target, not gold-plating. — REVERSIBILITY: moderate.
[s8] COUNCIL on plan: 0/5 OBJECT, 4/5 ENDORSE_WITH_CONCERNS (skeptic, architect, guardian, historian) + 1/5 ENDORSE (pragmatist). NO SAFETY (guardian explicit) → proceed with concerns folded. All five independently verified against LIVE alpha source that the six pr_resolver branches (L132 bitbucket, L153/L161 azure, L198 _http_get success, L200 HTTPError, L384 _pr_head_refspec None) and the two validate_marketplace continue arms (L182 skills, L202 agents) are GENUINELY uncovered (existing tests hit only the URLError path + the command-loop continue). RIGHT-SIZING [pragmatist]: DO NOT SPLIT — two disjoint test files but one cohesive coverage-closing unit feeding the same downstream s7 ratchet; splitting doubles overhead for zero risk reduction. FOLDED concerns: (a) [guardian+architect] step 4 _http_get HTTPError test — pass a SENTINEL token in the headers= arg and assert the sentinel is ABSENT from str(exc) (mirror TestTokenNeverLeaks), converting "no headers passed" into the real "secret in header never leaks to error text" invariant; urlopen mocked so no network either way; (b) [architect+pragmatist] step 5 — the positive _pr_head_refspec refspec paths (github/azure/bitbucket) are ALREADY covered by existing TestResolveDiff cases; keep only the L384 return-None assertions (positive pins optional, framed as reinforcement not gap-closure); (c) [all] step 2 — drop the speculative `pi <= gi` Azure case (collapses into the same L153 raise already covered); use dev.azure.com/org/_git/repo/pullrequest/9 (gi=1) for L161 and the two other URLs for L153; (d) [historian+architect+skeptic] steps 6/7 — assert the EXACT existing error strings ("missing YAML frontmatter" / "unterminated frontmatter block") with the any("...md" in e and "<msg>" in e for e in errors) idiom as POSITIVE behavioral assertions (not assert-nothing "no crash" line-hits); (e) [historian] place new pr_resolver classes (TestHttpGet, TestPrHeadRefspec) UNDER the existing section banners with a `# ... (slice s8)` banner, not at the file tail; (f) [architect] step 3 assert Request method=="GET" (read-only invariant, matches existing READ-verb asserts). — REVERSIBILITY: high.
[s9] COUNCIL on plan: ENDORSE (no OBJECT, no SAFETY) — pragmatist ENDORSE (keep-as-one; six task groups are one cohesive independently-shippable change, one file, one gate pass/fail, no internal dep ordering → NOT a split), skeptic+architect+guardian+historian ENDORSE_WITH_CONCERNS. Proceed with concerns folded. FOLDED: (a) [architect+skeptic, critical] resolve_within L95/L97 `except ValueError` is UNREACHABLE via crafted relpath on POSIX (both commonpath args are realpath'd absolute before the call; no cross-drive case on macOS/Linux) — plan updated to trigger it by mock.patch("dashboard_server.os.path.commonpath", side_effect=ValueError) and assert the deny-on-error security contract (returns None); this forces the deny branch and does NOT stub a security primitive (real isabs/null-byte/realpath still run); dropped the non-executable cross-drive fallback; (b) [skeptic] re-anchor targets to a fresh full-suite trace, don't book already-green lines as coverage wins, and record that ~ two dozen remaining uncovered lines are def/class headers + module constants (structurally uncountable under measure_coverage's documented import-time under-reporting bias) so 100% is not the bar; (c) [architect] L467/L483-487 need a MULTI-root build_server([a,b],...) fixture (single-root make_handler short-circuits _owning_root at L481-482 and never reaches partition/match). — REVERSIBILITY: high (tests only).
[s9] TRACE RECONCILED (skeptic concern): ran measure_coverage's exact production path (_load_suite + _run_under_trace, full 281-test suite) on alpha tip → dashboard_server.py 271/376 = 72.1%, gate PASS. Authoritative genuinely-uncovered runtime-coverable set: L95,L97 (resolve_within except/return None), L168 (raise slices-not-a-list), L231 (waves break), L301 (iter-headers continue), L325 (parse-header no-]), L337 (header_state ""), L346 (has_filled_answer False), L397/L400/L415 (do_GET/do_HEAD/_reject_method, invisible to per-thread trace via daemon serve_forever), L467 (resolve_run_dir "",None), L483/L486/L487 (owning_root partition+match+miss), L496 (serve_static 404). NOTE: the skeptic's independent re-run reported 8 of these as already-green; my reproduction of the tool's OWN discovery+trace path shows them uncovered — I trust the production-methodology reproduction and target the full set for robustness (every added test asserts REAL behavior, so even a line already green under some trace ordering still earns a genuine behavioral assertion, never a hollow line-hit). — REVERSIBILITY: high.
[s9] REVIEW (Tier-3: pr-test-analyzer + silent-failure-hunter + code-reviewer): NO P0/P1 (blocking bar). silent-failure-hunter — no false-green: verified both mocks force genuinely-unreachable defensive branches while the REAL primitives run (commonpath-raise reaches resolve_within's deny-on-error after real isabs/null-byte/realpath; _read_asset->None reaches _serve_static's body-is-None 404 only after real resolve_within+isfile), and every in-thread verb-entry test runs the REAL _route/_host_allowed/_emit (nothing security-relevant stubbed). code-reviewer — meets standards; 1 info note (POSIX-only endswith("/"), consistent with the file's existing POSIX assumptions, not actionable). pr-test-analyzer — ready; P2 recommendation FOLDED (below-bar but cheap + security-relevant): added test_do_get_foreign_host_emits_421_on_wire — an in-thread do_GET->_route->_host_allowed 421 mirror of the socket suite's anti-DNS-rebinding deny, closing the same daemon-thread trace-visibility gap. Other P3s (bundled _has_filled_answer cases, y-label symmetry) noted, below bar, not blocked. No auto-fix loop needed. — REVERSIBILITY: high.
[s8] REVIEW (Tier-2: code-reviewer + pr-test-analyzer): PASS — 0 P1 from either; both independently mutation-tested all 8 target branches and confirmed the tests are LOAD-BEARING (not tautologies/line-flippers): breaking _http_get's `return resp.read()` fails the success test; removing the skills/agents-loop `continue` (validate_marketplace L182/L202) fails both FrontmatterSkipBranchTest cases (AttributeError on None.get); dropping the azure gi<1 clause fails the git-first-segment test after the fix (below). ONE P2 FOLDED (auto-fix pass 1, receiving-code-review discipline): [test-analyzer] test_azure_git_as_first_segment_rejected used dev.azure.com/_git/... which EXECUTES L153 but is NOT mutation-sensitive to the `gi < 1` sub-clause — with gi<1 removed, that input falls through to L161's dev.azure.com `gi < 2` guard and still raises the same "Azure DevOps" message, so the comment overstated the guarantee. FIX: switched to the .visualstudio.com form (org is the SUBDOMAIN, so L161's dev.azure.com-only guard never applies) → gi<1 is now the ONLY guard that can fire; VERIFIED mutation-sensitive (removing gi<1 → "ResolverError not raised" failure; restored → pass) and L153 still COVERED. Below-bar notes (recorded, NOT changed): (P3 both) test_http_error_message_never_leaks_auth_header is a forward-protection guard, not a current-branch driver — L200's message is built only from code/url/reason and structurally cannot contain the header today (matches the existing TestTokenNeverLeaks defensive philosophy; the sibling test_http_error_raises_actionable_with_code_and_url is the actual HTTPError-arm pin); (P3 test-analyzer) the frontmatter skip tests guard via an uncaught AttributeError rather than a own-assertion mismatch — still catches any regression removing the guard, informational; (P3 test-analyzer) the azure `pi <= gi` sub-clause (pullrequest before _git) remains untested — unusual malformed shape, collapses into the same L153 raise, optional, not added (matches the s8 plan's deliberate no-contrived-URL decision). Over-coupling: minimal — req.get_method()=="GET" is stable public API pinning the read-only invariant; the urlopen context-manager mock is contract-shaped. STDLIB ONLY confirmed (no third-party imports). — REVERSIBILITY: high.
[s9] SIMPLIFY (code-simplifier, non-blocking): no changes — added tests already clear/idiomatic and match the file's deliberate conventions (explicit self-contained wire assertions, in-thread make_handler mirror, real primitives with mocks only forcing defensive branches). Every candidate edit would weaken/relocate a behavioral assertion or diverge from convention for negligible gain. Suite re-verified 91/91 OK.
[s9] QUALITY-GATE: PASS (no refactor pass needed). Measured all added test methods/classes in scripts/test_dashboard_server.py against ~/.claude/spec-loop/quality-gate.json thresholds: MAX method_lines=22 (<=50), MAX cyclomatic=7 (<=10, cognitive bounded well under 15), MAX params=0 (<=4), MAX nesting=1 (<=3), MAX class_lines=52 (MultiRootHandlerUnitTests, <=300). All flat, single-purpose test functions. Within every threshold.
[s8] SIMPLIFY (code-simplifier, non-blocking): NO CHANGE — the s8 test additions already meet the clarity/DRY bar and match the files' conventions (mock.patch("pr_resolver..."), m.call_args.args[0] idiom, run_on_frontmatter fixture, # --- section banners). The two TestHttpGet helpers (_urlopen_cm, _http_error) are each reused 2-3x and carry non-obvious rationale (addCleanup(err.close) prevents a Python 3.14 ResourceWarning from HTTPError's internal file object); further abstraction would over-engineer three short tests. All constraint-protected assertions intact (no-leak, req.get_method()=="GET" read-only, exact error strings, bypass negatives). 58 + 51 file tests pass.
[s8] QUALITY-GATE: PASS (no refactor pass needed, 0/3 used). Measured all added test methods/helpers/classes via AST (thresholds cc<=10, cognitive<=15, method_lines<=50, params<=4, nesting<=3, class_lines<=300, crap<=30): worst cyclomatic 5 (the two FrontmatterSkipBranchTest methods, from their assertTrue+assertFalse any(...)-with-and predicates), worst method 18 lines, worst params 3 (_http_error: self+url,code,reason net 3), worst nesting 2 (assertRaises+with mock.patch), worst class 45 lines (TestHttpGet). CRAP: trivially < 30 — these are fully-executed test methods (cc<=5), so coverage≈100% drives CRAP toward the raw cc. Tests-only change; no production complexity introduced. — REVERSIBILITY: high.
[s8] MERGE: slice merged into alpha via --no-ff (merge commit 8cfd864; slice commit 3b35c42). Sibling slice s9 (dashboard_server residual gaps) had advanced alpha (d449ae7 -> 6856da1) meanwhile, touching only test_dashboard_server.py + its plan — disjoint from s8's files; merged alpha into the slice branch first (clean, ort strategy), re-verified (310 tests, gate PASS, s8 targets held: pr_resolver 82.8%, validate_marketplace 84.1%; s9's dashboard_server 77.0% present), then integrated. Post-merge on alpha: 310 tests OK, coverage gate PASS (TOTAL 79.2%, 962/1215), marketplace validates OK. Worktree removed, slice branch deleted. PUSH to origin/alpha NOT completed — environment lacks git push credentials (HTTPS "could not read Username"; same as all prior slices, integrated locally); controller/human to push. — REVERSIBILITY: high.
[s8] DONE — pr_resolver.py 80.7%->82.8% (6 genuine branches: L132/L153/L161/L198/L200/L384), validate_marketplace.py 83.2%->84.1% (2 continue skip-branches L182/L202); test-only, stdlib-only, no network/CLI; merge 8cfd864. No production source changed (main(argv=None) already present).
[s7] COUNCIL: OBJECT — 3/5 object (Architect, Skeptic, Guardian; none SAFETY. Pragmatist ENDORSE — one shippable slice, not a split; Historian ENDORSE_WITH_CONCERNS — rewrite the stale "under-report is safe direction" docstring L22-31, follow s7-marker test-placement convention). Majority-OBJECT halts per iron-council aggregation (>=3/5) — NOT autonomously foldable (fold-in is only for ENDORSE_WITH_CONCERNS / minority). Objections converge on ONE fixable design flaw: plan step-1 ordering (purge+reimport targets AFTER main() already discovered/imported them) splits each target into two live module identities — measured 310 tests/0 fail baseline vs 12 err/6 fail under the plan's ordering; the suite goes red and the gate can never report improved coverage; 54 string-target mock.patch sites would also patch the wrong object. Guardian adds: floors "rounded down ~2-3 pts" from LOCAL py3.14 are too thin for CI py3.12 co_lines() drift (false-red wedge). Proven remedy (Architect): move discover() INSIDE tracer.runfunc AFTER the fresh re-import so the suite binds the single traced copy (stays green, import-time lines attributed). Escalated to human via escalation-gate trigger council-objection; NOT executing the plan while objected-to. Also noted for whatever plan proceeds: stale 188-test/54.0% baseline block must refresh to current 310-test/79.2% reality. Returning NEEDS_DECISION. — REVERSIBILITY: n/a (no code changed; plan not executed).
[s8] DONE — pr_resolver.py 80.7%->82.8%, validate_marketplace.py 83.2%->84.1%; merge 8cfd864; test-only, mocked urlopen, tmp-dir fixtures.
[s9] DONE — dashboard_server.py 72.1%->77.0% (gate); merge 6856da1; test-only (source byte-for-byte unchanged), Tier-3 clean, security asserts intact.
[wave3] INTEGRATION CHECK: 310 Python tests pass, coverage gate PASS (TOTAL 79.2% production tool), Node harness 15/15.
[s7] COUNCIL: OBJECT 3/5 (architect/skeptic/guardian, none SAFETY) on the PLAN's fix ordering (module-identity split; thin floor margin). Lifted to human per escalation-gate (council-objection); NOT decided autonomously.
[s7] HUMAN RESOLVED — Option 1: apply the council's proven remedy (defer discover() inside tracer after fresh re-import → one module identity; RED identity+attribution test; ratchet margin ≥5pts for py3.12 drift; refresh stale baseline+docstring). Re-dispatching s7 with remedy injected; council NOT re-convened (human-adjudicated).
[s7] RE-DISPATCH: applied resolution; council NOT re-convened (human-adjudicated). Not split — measurement+enforcement+docs are one atomically-shippable change (correct tool + its floors + docs must land together), correctly sized. — REVERSIBILITY: high.
[s7] SEAM CORRECTED: _run_under_trace() now, INSIDE tracer.runfunc, (1) re-imports targets fresh (pop+import_module, replacing sys.modules[name]) then (2) discover() then (3) run — single traced module identity. RED test written first (TracedRunTests) asserting (a) suite green + tests_run>=MIN_TESTS, (b) release.py L39 import-time constant now COUNTED, (c) sys.modules['pr_resolver'] IS the object test_pr_resolver bound; failed against old discover-before-trace, passes after. BEFORE (old under-report, import-before-trace): launcher 72.4%, server 77.0%, pr_resolver 82.8%, release 82.4%, validate 84.1%; TOTAL 79.2%. AFTER (corrected, minus OMIT): every file 100.0%, TOTAL 100.0% (1214/1214), 313 tests green. — REVERSIBILITY: high.
[s7] RECURSION GUARD: the tool runs its OWN scripts/test_*.py suite under trace, which includes TracedRunTests, which spawns the tool as a subprocess to assert on its COVPROBE line — un-guarded this fork-bombs (observed 56 procs). Fix: _run_under_trace sets env MEASURE_COVERAGE_ACTIVE=1 (restored in finally) while the traced suite runs; TracedRunTests.setUp skips when it sees that env (it is inside a tool run — assertions run only at top-level unittest). Verified bounded (no fork bomb), full suite 313 OK, seam class 3/3 ok standalone. — REVERSIBILITY: high.
[s7] FLOORS ratcheted with ≥5pt margin (py3.12 CI vs py3.14 local, no py3.12 locally to validate co_lines() drift): launcher 63->95, server 42->95, pr_resolver 78->80 (from human's py3.12 preview 85.4%−5, NOT local 100% — ~40 genuinely-unhit lines on 3.12 are a real healthy floor, not chased here), release 0->95, validate 51->94; TOTAL 51->90 (py3.12 aggregate ~96-97% pr_resolver-dragged, minus margin). Margin rationale recorded in-code + validate.yml comment. — REVERSIBILITY: high (floor integers).
[s7] OMIT finalized: fixed STALE validate_marketplace shim 312-313 -> 313-314 (source drift; was leaving L314 sys.exit unhit → 99.5%); every excluded line now carries per-line rationale — five if __name__/sys.exit(main()) shims (launcher 551-552, server 663-664, pr_resolver 488-489, release 194-195, validate 313-314) + serve_forever try/except/finally daemon tail (server 654-659). Rationale-required + malformed + overshoot + 25%-cap guards intact (verified all ranges valid, within cap). — REVERSIBILITY: high.
[s7] ARTIFACTS refreshed: replaced stale 188-test/TOTAL-54.0% baseline block with 313-test/100.0% corrected reality; rewrote the "biases toward UNDER-reporting (safe direction)" docstring to describe corrected attribution (import-time lines now counted via under-trace re-import; residual gap is only the OMIT'd shims/serve_forever tail); MIN_TESTS comment updated 188->313; validate.yml JS-coverage "deferred to s7" note replaced (Node 20 report-only, intentional). Added README "Test coverage" section (how measured py+JS, exclusion policy, floors+≥5pt margin, run-both-locally). — REVERSIBILITY: high.
[s7] FAIL-CLOSED verified (local dry-run, reverted): Python gate rc=1 on a floor perturbed to 101% (then reverted to 80); MIN_TESTS<150, runaway-OMIT>25%, overshoot-OMIT, and missing-rationale all raise/return 1; JS gate min-count guard trips at 5<12. Both gates fail closed. s1 empty-suite/runaway-OMIT guards preserved unchanged. — REVERSIBILITY: high.
[s7] RESUMED after session-limit cutoff — worker's verified work was uncommitted in its worktree (313 tests pass, corrected tool 100% attribution, fail-closed confirmed, guards intact, remedy correctly applied). Controller committed (7b71ae5) + merged (00de840) the interrupted slice after independent verification; per-slice review deferred to the Phase 5 cross-slice review.
[phase5] INTEGRATION GATE: PASS. (1) Full suite 313 tests + honest coverage gate 100.0% per-file (1214/1214, floors 80-95%/90% met) + Node 15/15 + marketplace validation OK on assembled alpha. (2) Tier-3 cross-slice review over ac3759b..HEAD: CONFIRMED 100% is honest (OMIT = only __main__ shims + serve_forever tail; validate_omit + 25% cap guards), module-identity fix sound (identity_holds), all 3 CI steps fail-closed, JS harness leaves page standalone, floors are documented py3.12 margins. One P2 (CI redundancy, no fix required). No remediation slice needed. RUN COMPLETE.
