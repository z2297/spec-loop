# Decisions Log — 20260629-docker-dashboard

Append-only. One line per autonomous decision made on the human's behalf.

[intake] COUNCIL: OBJECT — 3/5 object (skeptic, pragmatist, historian); architect+guardian ENDORSE_WITH_CONCERNS. No SAFETY blocker. Lifted to human per escalation-gate (council-objection). See escalations.md.
[intake] HUMAN RESOLVED — Docker preferred + python fallback; cross-repo/machine-wide aggregation; idempotent singleton + explicit --stop. Overrides pragmatist/historian OBJECT on Docker (fallback preserved) and on scope (cross-repo accepted). See escalations.md answer.
[intake] DECISION: reconciled Q2 (machine-wide) with Q3 (worded per-repo) as a SINGLE machine-wide singleton container (fixed deterministic name), idempotent start/reuse + explicit --stop, each repo registers its root. — RATIONALE: Q2 scope dominates; Q3 chosen for its lifecycle mechanism (idempotent, start-and-exit, no reaper), which applies cleanly to one machine-wide container. — REVERSIBILITY: moderate.
[s1] COUNCIL: ENDORSE_WITH_CONCERNS — skeptic/architect/pragmatist/historian EWC; guardian OBJECT but EXPLICITLY not-SAFETY (resolve_within remains defense-in-depth). Single non-SAFETY objection is not a council OBJECT; folded all concerns into the plan and proceeding.
[s1] FOLDED (architect/guardian/historian): _resolve_run_dir + _collection_material generalized per-root; split namespaced id on FIRST ':' only, gate against OWNING root's known-id set, confine remainder via resolve_within(owning_data_root,...) — never call resolve_within against a sibling root. Added adversarial test: crafted id 'rootA:../rootB/run-x' (and ':'/ '/' in a basename) must 404 uniformly (no path oracle) and never read a sibling root.
[s1] FOLDED (skeptic/guardian): _host_allowlist stays hardcoded to 127.0.0.1/localhost host strings; bind_host is NEVER fed into the allowlist. Test asserts allowlist contains NO 0.0.0.0:* entry AND a foreign/'*' Host still 421s when bound 0.0.0.0.
[s1] FOLDED (historian/architect): avoid build_server param-count breach (quality-gate params<=4) by keeping bind_host/advertise_port keyword-only with defaults (surface stays low) rather than a 5-positional signature; scan_runs(docs_root) single-arg contract kept intact, scan_all_roots added alongside.
[s1] FOLDED (historian/guardian): single-root /api/runs stays byte-for-byte — bare run_id, no 'root' field, unchanged {"runs":[...]} envelope; namespacing + 'root' field only when >1 root. Guarded by a regression test written before generalizing (step 3).
[s1] FOLDED (guardian): root_key ordering/de-dup must be deterministic & stable across runs — derive from sorted roots, index-suffix collisions deterministically; test pins stability. --advertise-port CLI flag KEPT (named in slice goal (a)); pragmatist's YAGNI note logged but goal explicitly requires the flag, so retained.
[s1] REVIEW (Tier-3, 3 reviewers): code-reviewer + silent-failure-hunter + pr-test-analyzer all NON-BLOCKING; all 5 security invariants independently verified (allowlist never widened by 0.0.0.0, GET/HEAD-only, per-root confinement, no-path-oracle 404, single-root byte-for-byte). Zero findings at/above the Tier-3 bar.
[s1] REVIEW FIXES APPLIED (below-bar but cheap + on the security path): (1) advertise_port=0 unreachable-allowlist edge closed — effective advertised port now falls back to real bound port for falsy 0/None [code-reviewer]; (2) added tests: bogus/empty/bare root-key -> uniform no-oracle 404, concrete de-dup suffixes (repo#1/repo#2), single-root colon-in-run-id transparent resolution, advertise_port=0 fallback [pr-test-analyzer #1-#4]; (3) comment on intentional ETag '-' degrade [silent-failure-hunter LOW].
[s1] REVIEW LOGGED-NOT-FIXED (below bar): _read_text_capped conflates absent-vs-unreadable — PRE-EXISTING (inherited, not introduced by this diff), outside slice scope [silent-failure-hunter MEDIUM]; main() redundant root resolve (cosmetic); MAX_RUNS now caps the aggregate (by design) [code-reviewer]. Tests now 47/47.
[s1] SIMPLIFY (non-blocking, code-simplifier): removed duplicated owning-root logic (deleted _root_key; _owning_root now single source of truth for key+data_root+run_id; _resolve_run_dir returns (root_key, run_dir)); de-duplicated main() root stringification. Public signatures unchanged; 47/47 pass. Behavior preserved.
[s1] QUALITY-GATE: initial FAIL — build_server params=5 > 4 (the historian/architect-flagged concern). Refactor attempt 1/3 (behavior-preserving): grouped bind_host+advertise_port into a NetworkConfig namedtuple (mirrors the existing Response value-object convention), dropping build_server to 4 params. Re-measure: PASS — all changed functions within thresholds (cc<=10, cognitive<=15, lines<=50, params<=4, nest<=3), DashboardHandler 142<300 lines. Tests 47/47 (assertions & behavior unchanged; only the network-arg construction ergonomics of the 4 new tests changed).
[s1] RIGHT-SIZE: KEEP AS ONE slice — RATIONALE: bind/allowlist (Part A) and multi-root (Part B) both edit build_server/main on the same file and are naturally sequential TDD edits (steps 1-2 then 3-5); pragmatist flagged borderline but leaned keep-as-one; dispatch guidance prefers one slice. Not splitting.
[intake] DECISION (folded council concerns into decomposition): (a) python fallback kept first-class [pragmatist/historian]; (b) Dockerfile CMDs the EXISTING dashboard_server.py, no parallel server, reuse build_server/main + --port/--root [historian single-source-of-truth]; (c) must-not-regress security invariants bound to every slice — publish 127.0.0.1:PORT:PORT only, closed Host allowlist (no `*`), artifacts mounted :ro, per-repo-scoped/named teardown (no unscoped rm -f), non-root + --cap-drop ALL, no --privileged, no docker.sock mount [guardian]; (d) bind host decoupled from Host allowlist — allowlist derived from advertised/published port [architect/guardian]; (e) multi-root uses MINIMAL per-root :ro mounts, NO broad $HOME mount; per-root realpath/commonpath confinement preserved [guardian/historian]; (f) new launcher/lifecycle logic covered by pure-stdlib unittest matching test_dashboard_server.py; Docker-daemon-dependent tests guarded/skipped (validate.yml has no Docker) [historian]; (g) dashboard-serve.md rewritten to honestly reflect the dual-use `docker`-invoking Bash surface [guardian]. — RATIONALE: iron-council intake feedback. — REVERSIBILITY: moderate.

[s4] COUNCIL: ENDORSE_WITH_CONCERNS — historian ENDORSE; skeptic/architect/pragmatist/guardian EWC; zero OBJECT, zero SAFETY. Plan worthy; folded cheap concerns and proceeding (no escalation).
[s4] FOLDED (pragmatist): collapsed the standalone regex-extract JS unit harness into the single integration script s4_verify.mjs — verify the 12-line pure groupRunsByRoot ONCE (against synthetic + server-produced JSON) instead of two overlapping harnesses. Kept the pure-helper/render split (right cheap seam). No committed JS harness (out-of-tree scratchpad only, per decisions-log:20(f)).
[s4] FOLDED (guardian): added an XSS-safety assertion at the render boundary — hostile root "<img src=x onerror=...>" through rootGroupSection's el("h3",{text}) must render as literal textContent with children.length===0. Turns the textContent-only invariant into a tested fact rather than inferred.
[s4] FOLDED (architect): corrected plan comment/claim — server emits roots in INPUT-ARGUMENT order (not "sorted"); _resolve_roots/_root_keys preserve input order, scan_all_roots emits per-root sequentially. Client first-appearance grouping matches this and is robust regardless.
[s4] FOLDED (skeptic): (a) confirmed the <h3> label is the RAW root key verbatim (human-meaningful, collision-free repo basename, #2-disambiguated by s1) as the intended "group/label by root" affordance — deliberately no path hint; documented so reviewer won't re-litigate. (b) Task 2 asserts server suite GREEN, not a brittle fixed 47/47 count.
[s4] NOTE (skeptic/architect/guardian): the mixed-root-presence test guards a CONTRACT VIOLATION (server namespaces all-or-nothing), not a real API shape; kept as defensive-only. Empty-root groups cannot occur (scan appends only existing runs) so no empty <h3> chrome renders.
[s4] RIGHT-SIZE: KEEP AS ONE slice (depth 0) — pragmatist confirms one ~20-line edit to one file with its verification; Task 2 modifies no files (verification for Task 1). Nothing independently shippable to split. Not splitting.
[s4] VERIFY (pre-review evidence): out-of-tree node s4_verify.mjs 13/13 (pure grouping single/multi/extra-colon/mixed + XSS textContent invariant) PASS; live python s4_live.py against merged dashboard_server — single-root JSON has bare ids/NO root field, multi-root JSON has namespaced ids + root + distinct keys for same-named cross-repo runs (collision de-dup) PASS; s4_render.mjs feeds REAL served JSON through client renderOverview (DOM shim): single-root => flat .run cards, ZERO .section/h3; multi-root => one .section>h3 per root labeled by key, cards bucketed correctly PASS. Server suite green (47 tests, OK) — untouched.
[s4] REVIEW (Tier-2, pr-review-toolkit code-reviewer, sequential): ZERO findings at any severity (P0/P1/P2/P3). All six hard constraints independently verified upheld (read-only/GET-only, textContent-only for root+id, ids verbatim never split, single-root byte-for-byte, self-contained+POLL_MS, freshness+drill-down intact). No auto-fix loop needed (nothing at/above P0,P1 bar).
[s4] SIMPLIFY (non-blocking, code-simplifier): NO CHANGE — groupRunsByRoot/rootGroupSection/renderOverview branch already clean and idiomatic (Object.create(null) map, (x||[]) guard, el/.section/h3 reuse matching 5 existing detail sections). The !multiRoot early-return is load-bearing (preserves single-root byte-for-byte); any edit would be cosmetic or risk a constraint. Behavior preserved.
[s4] QUALITY-GATE: PASS (no refactor needed). All three changed JS functions within thresholds (cc<=10, cognitive<=15, method_lines<=50, params<=4, nesting<=3): groupRunsByRoot ~cc6/cog6/12 lines/1 param/nest2; rootGroupSection ~cc2/6 lines/1 param/nest1; renderOverview ~cc5/16 lines/1 param/nest1. No class (class_lines n/a). No custom gates.
[s2] COUNCIL: ENDORSE_WITH_CONCERNS — skeptic/architect/guardian EWC, pragmatist ENDORSE, historian ENDORSE (0 OBJECT, no SAFETY). Proceeding, folding concerns. Pragmatist confirms ONE independently shippable change — NOT split (correctly sized, depth 0).
[s2] FOLDED (skeptic/architect/guardian): .dockerignore re-include scoped to EXACTLY scripts/dashboard_server.py + scripts/dashboard_assets/ (deny-all then those two paths only), NOT blanket !scripts/ — so tests/pr_resolver.py/release.py/validate_marketplace.py/__pycache__ never enter the build context. Dockerfile COPY already scoped to the two runtime paths (the real gate on image contents).
[s2] FOLDED (skeptic/architect/guardian): Dockerfile run-contract header comment documents that with the default CMD (no --root), cwd defaults to /app and /app/docs/spec-loop does NOT exist -> server starts healthy (stderr warning) and serves zero runs until s3 bind-mounts artifact roots :ro and passes --root <mounted-path>. Prevents an empty dashboard being mistaken for a defect.
[s2] FOLDED (historian): CMD verb kept as python3 (not python) to match repo convention (script shebang, CI, docstring usage); do not let simplify swap it. python:3.12-slim base matches the ONLY Python CI exercises (validate.yml pins 3.12).
[s2] FOLDED (skeptic): report states the verification path HONESTLY (docker build+--help output, or 'no daemon; review-only'); image digest-pinning deliberately OUT OF SCOPE for this minimal slice (unpinned 3.12-slim is intentional, not an oversight).
[s2] REVIEW (Tier-2, code-reviewer on Dockerfile + .dockerignore diff): NO P0/P1 findings (blocking bar clean). Reviewer independently traced .dockerignore last-match-wins semantics (scripts/__pycache__ stays excluded; dashboard_server.py + dashboard_assets/** re-included; tests/pr_resolver.py/release.py/validate_marketplace.py excluded) and confirmed CMD flags + allowlist claims against dashboard_server.py.
[s2] REVIEW FIXES APPLIED (below-bar, cheap): P3 comment imprecision fixed — Dockerfile asset-resolution comment now says Path(__file__).resolve().parent (matches code line 568) instead of Path(__file__).parent. LOGGED-NOT-FIXED (below bar / by design): P3 base tag not digest-pinned (reproducibility explicitly out of scope for minimal local image); P2 run contract enforced by s3 not the image (intended division of labor, documented in header).
[s2] LOCAL VERIFICATION (docker daemon available): docker build OK; container runs uid=10001(app) non-root; only dashboard_server.py + dashboard_assets/index.html baked (tests/docs/other scripts absent); server --help resolves; -p 127.0.0.1:8787:8787 -> GET / 200 + /api/runs {"runs":[]}; foreign Host -> 421 (allowlist NOT widened by 0.0.0.0 bind); with --advertise-port matching a differently-published host port -> 200 (proves s3's runtime contract).
[s2] SIMPLIFY (non-blocking, code-simplifier): no changes — Dockerfile + .dockerignore already clear/minimal. Simplifier explicitly declined to "tidy" the order-dependent .dockerignore recursive re-include block (behavior risk, conflicts with do-not-broaden constraint). CMD python3/--bind-host 0.0.0.0/no --advertise-port, non-root uid 10001, run-contract header all preserved.
[s2] QUALITY-GATE: PASS (vacuous). Changed files are Dockerfile + .dockerignore — declarative infra with NO measurable code units (no functions/methods/classes; cyclomatic/cognitive complexity, method_lines, parameter_count, nesting_depth, class_lines, crap_score all N/A). No thresholds apply; no code was added to scripts/. No refactor loop needed. (No config edit — thresholds untouched.)

[s3] COUNCIL: ENDORSE_WITH_CONCERNS — skeptic/pragmatist/guardian/historian EWC; architect OBJECT (non-SAFETY, explicitly). 1/5 object, no SAFETY => not a majority/SAFETY halt => council verdict is ENDORSE_WITH_CONCERNS. Folded ALL concerns (incl. the architect's correctness defect) into the plan and proceeding; no escalation.
[s3] FOLDED (architect BLOCKER — correctness): server's _resolve_roots appends docs/spec-loop to --root, so mounting host <root>/docs/spec-loop at container <mount> and passing --root <mount> would make the server search <mount>/docs/spec-loop and serve ZERO runs (shape-only tests would pass green). FIX: mount host <root>/docs/spec-loop at container <container-root>/docs/spec-loop and pass --root <container-root> (matches the s2 Dockerfile contract "mount repo root, --root /repo", but minimal — only the artifacts dir crosses). Added a mount/--root COMPOSITION test (step 4) asserting target == <container-root>/docs/spec-loop for the matching --root, not just that -v ends :ro.
[s3] FOLDED (architect): added parse_image_present + build_image_argv(tag, context_dir) to the tested layer (IMAGE_TAG=spec-loop-dashboard:local; s2 baked no tag); build context pinned to THIS repo's checkout (the dir holding the Dockerfile), never an arbitrary machine-wide cwd. Added parse_all_names (docker ps -a) so an exists-but-STOPPED singleton triggers scoped rm-then-run, not a bare run that errors on the name conflict. Registry timestamp written ONLY after a successful run/reuse.
[s3] FOLDED (guardian): step-5 build_run_argv asserts strengthened — assert the -p host-side port token EQUALS the --advertise-port token (value coupling, not just presence); assert --advertise-port value is never '*' and --bind-host appears only as 0.0.0.0 (launcher never widens the s1 allowlist). Registry (~/.spec-loop/dashboard/) is HOST metadata only, NEVER in the mount list. main() case: a docker run NAME-CONFLICT exit => reuse+exit 0 (never python fallback, never unscoped rm -f).
[s3] FOLDED (skeptic): dashboard-serve.md states that registering a NEW repo RECREATES the shared singleton (brief browser blip — the honest realization of "attach + live updates from all sessions"); documents the concurrency posture (Docker --name uniqueness is the singleton lock; registry read-modify-write is best-effort, lost updates tolerated, a dropped root re-registers next launch); notes first invocation may build the image (tens of seconds); python fallback does NOT register a root (registry = the singleton's mount set; prune_stale reaps orphans).
[s3] FOLDED (pragmatist/historian): DROP the --port launcher flag (YAGNI for a fixed-name/fixed-port machine-wide singleton — fights the reuse-by-name model; s3 goal never names it, unlike s1's --advertise-port). Keep DEFAULT_PORT constant mirroring dashboard_server.DEFAULT_PORT (8787) — import/mirror, do not re-declare. Share one scoped stop+rm helper between --stop and RECREATE. Reuse s1's root_key as the container-root leaf (no parallel naming scheme). Model _run(argv) verbatim on scripts/pr_resolver.py::_run (shell=False, check=False, capture_output=True, text=True, FileNotFoundError guard). Test structure follows test_pr_resolver.py mock.patch(subprocess.run) + argv-recording fake.
[s3] RIGHT-SIZE: KEEP AS ONE slice (depth 0) — pragmatist right-sizing finding: launcher + command-doc rewrite are coupled by a correctness invariant, not convenience (doc documents launcher behavior => vaporware if shipped first; launcher has no other caller => dead code without the doc; shipping the launcher without the rewrite leaves dashboard-serve.md violating the DOCS-HONESTY constraint about the now-docker-invoking Bash surface). A reviewer rejecting "the doc" should request an in-slice doc revision, not a split. Consistent with s1/s2/s4 all kept-as-one. Not splitting.
[s3] VERIFY (pre-review evidence): launcher unit suite 53 tests OK (no daemon); full suite 165 OK. Local docker-path proof (daemon up): CREATE -> serves repoA run (proves mount/--root composition: -v <root>/docs/spec-loop:/roots/<key>/docs/spec-loop:ro + --root /roots/<key>) -> REUSE (no restart) -> RECREATE on repoB registration -> /api/runs aggregates BOTH roots namespaced (repoA:*, repoB:*) -> --stop removes the singleton. Inspect confirmed CapDrop=[ALL], User=app, Privileged=false, mounts RW=false, -p 127.0.0.1:8787:8787, --advertise-port 8787.
[s3] REVIEW (Tier-3, 5 reviewers: risk/design/tests/correctness + silent-failure-hunter): risk APPROVE (no SAFETY — all 5 hard invariants verified in build_run_argv/teardown code, not just the run); design APPROVE_WITH_COMMENTS (3xP2 layering/doc); tests APPROVE_WITH_COMMENTS (3xP2 coverage gaps); correctness REQUEST_CHANGES + silent-failure: 3 findings at/above the P0+P1 bar.
[s3] REVIEW FIXES APPLIED (auto-fix attempt 1/2 — all at/above-bar findings, verified empirically): (P1) docker installed-but-daemon-down crashed with IndexError (available==False bypassed the `is None` fallback -> plan_launch (FALLBACK,[]) -> _execute_plan argvs[-1] on []); FIX: any falsy availability falls back with distinct "daemon not responding" reason + _run_plan/_execute_plan guard empty argvs. (P1) _fallback used cwd-relative scripts/dashboard_server.py (broke from non-repo cwd) and captured a foreground server's output (hidden until exit); FIX: absolute server path via CONTEXT_DIR + absolute --root + os.execvp for live streaming/native Ctrl-C. (P1) read_registry/read_mount_set silently overwrote corrupt-but-present state (dropping other roots) and non-numeric timestamps crashed prune_stale; FIX: warn on corrupt (observable reset) + drop non-numeric values so pruning never crashes. Empirically re-verified: fallback from cwd=/ with docker absent serves live; docker-present-daemon-down falls back (no IndexError); full docker path (create/reuse/recreate/aggregate/stop) still works. Tests 53->60 launcher, 165->172 total.
[s3] REVIEW FIXES (below-bar, cheap, folded on the same paths): RECREATE tolerates benign "no such container" teardown race; _stop surfaces real stop/rm failures (returns nonzero) instead of always claiming success; module/_run docstrings corrected (real shell surface; _run returns CompletedProcess, adapted not verbatim); build_image_present_argv moved into the pure Layer A argv-builder section; _launch pre-registration doc corrected; mount-composition test strengthened to assert against dashboard_server's actual realpath(join(--root,*DATA_SUBPATH)) resolution.
[s3] REVIEW LOGGED-NOT-BLOCKING (below bar): risk P2 — the python-fallback path is inherently unhardened (no cap-drop/:ro/isolation) and leans on the s2 server's own loopback bind + Host allowlist; this is intrinsic to a fallback and NOT a new risk vs the pre-existing server. No action.
[s3] SIMPLIFY (non-blocking, code-simplifier): one dedup — parse_all_names had a byte-for-byte identical body to parse_running_names (docker ps and docker ps -a share the one-name-per-line format), so it now delegates (distinct public signature/docstring kept). Simplifier explicitly declined to churn the tests or collapse the security-critical argv builders / the belt-and-suspenders empty-argvs guard. 172/172 pass. Behavior preserved.
[s3] QUALITY-GATE: initial FAIL — plan_launch params=6 > 4 (all other metrics within thresholds: cc<=7, lines<=32, nest<=3, no class). Refactor attempt 1/3 (behavior-preserving): grouped the four live-docker-state inputs (daemon_available, image_present, running_names, all_names) into a DaemonState namedtuple (mirrors dashboard_server's Response/NetworkConfig value-object convention); plan_launch(state, current_roots, desired) => 3 params. Re-measure: PASS — every changed function within thresholds (cc<=10, cognitive<=15, method_lines<=50, params<=4, nesting<=3), no class. Tests 172/172 (assertions & decision logic unchanged; only arg construction at the caller + 6 test call sites changed, via a _state() helper). Docker path re-smoked (create/serve/stop). No config edit — thresholds untouched.
[s2] DONE — merge f77c787; minimal non-root python:3.12-slim Dockerfile + .dockerignore run the existing server; docker build+run verified locally; 47/47.
[s4] DONE — merge 72c73ce; index.html groups overview by root, single-root byte-for-byte unchanged; Tier-2 zero findings; 47/47.
[s3] DONE — merge ac3759b; dashboard_launcher.py (docker-preferred/python-fallback singleton) + rewritten dashboard-serve.md; architect OBJECT (non-SAFETY, double docs/spec-loop path) folded+fixed; Tier-3 5-reviewer APPROVE after auto-fix; 172/172; docker inspect confirmed 127.0.0.1 publish, :ro mounts, cap-drop ALL, non-root, scoped teardown.
[phase5] INTEGRATION GATE: PASS — full suite 172/172 + marketplace validation OK on assembled alpha; ONE Tier-3 cross-slice review over e590fc8..HEAD verified all 5 seams (launcher->server argv, CMD override, mount<->--root alignment, advertised<->published<->allowlist, server JSON->client) by reading AND booting end-to-end. No remediation slice needed. Run complete.
