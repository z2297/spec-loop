<!-- review-depth-map metadata -->
- **risk_tier:** 2
- **review-pr command:** `pr-review-toolkit:review-pr` (standard depth, changed-diff scoped)
- **simplify command:** `pr-review-toolkit:review-pr simplify`
- **blocking bar:** P1 (correctness/security) and P2 (maintainability/test-adequacy) findings block; P3 nits are non-blocking, logged only.
- **surface touched:** test-only additions to `scripts/test_pr_resolver.py` and `scripts/test_validate_marketplace.py`. No production source change planned (`pr_resolver.py` already has `main(argv=None)`; `validate_marketplace.py` untouched). No CI/gate config touched (s7's job).

# Slice s8 — close remaining logic gaps in pr_resolver.py + validate_marketplace.py

## Goal
Cover the GENUINE uncovered runtime branches (verified against the current `alpha` source via the project's own `measure_coverage.py` trace) with behavior-asserting, stdlib-only tests. NEVER hit the real network/CLI.

## Verified uncovered branches (from live trace on alpha)
`scripts/pr_resolver.py` — the only non-import/def-header executable lines missing:
- **L132** — `raise ResolverError("not a Bitbucket PR URL")` when a bitbucket.org path is not `.../pull-requests/<n>`.
- **L153** — `raise ResolverError("not an Azure DevOps PR URL")` in `_parse_azure_path` when the `_git`/`pullrequest` segment positions are invalid (`pi+1 >= len` OR `pi <= gi` OR `gi < 1`).
- **L161** — `raise ResolverError("not an Azure DevOps PR URL")` for the dev.azure.com `gi < 2` case (org missing before project/`_git`).
- **L198** — `return resp.read()` success path of `_http_get` (currently only the error paths at L200/L202 are exercised, via a URLError side-effect).
- **L200** — `raise ResolverError("HTTP <code> ...")` when `urlopen` raises `urllib.error.HTTPError`.
- **L384** — `return None` tail of `_pr_head_refspec` (no matching provider/pr_id/head_ref).

`scripts/validate_marketplace.py` — the two loop `continue` skip-branches:
- **L182** — `continue` in the `skills/*/SKILL.md` loop when `read_frontmatter` returns None (missing/unterminated block). Existing tests only exercise the *command* loop's continue (L195), never the skills loop.
- **L202** — `continue` in the `agents/*.md` loop when `read_frontmatter` returns None.

## TDD steps (test-only; assert OBSERVABLE behavior)
1. **pr_resolver — Bitbucket malformed URL (L132):** add to `TestParsePrUrl` a case asserting `pr.parse_pr_url("https://bitbucket.org/team/repo/wrong/7")` raises `ResolverError`. Assert the message names Bitbucket.
2. **pr_resolver — Azure malformed URLs (L153, L161):** add cases:
   - `https://dev.azure.com/org/proj/_git/repo/pullrequest` (no id after pullrequest -> `pi+1 >= len`) raises.
   - `https://dev.azure.com/_git/repo/pullrequest/9` (`gi < 1`, `_git` is first segment) raises.
   - `https://dev.azure.com/org/_git/repo/pullrequest/9` (`gi == 1` -> dev.azure.com `gi < 2` branch, L161) raises.
   - A `pi <= gi` ordering case, e.g. `.../pullrequest/9/_git/repo` style if reachable, else covered by the above.
   Assert each raises `ResolverError` naming Azure DevOps.
3. **pr_resolver — `_http_get` success (L198):** new test class `TestHttpGet`. Mock `pr_resolver.urllib.request.urlopen` to return a context-manager whose `read()` yields fixed bytes; assert `pr._http_get(url)` returns exactly those bytes. Assert the `Request` used method GET (via a recording mock) — read-only guarantee.
4. **pr_resolver — `_http_get` HTTPError (L200):** mock `urlopen` to raise `urllib.error.HTTPError(url, 404, "Not Found", {}, None)`; assert `_http_get` raises `ResolverError` whose message contains `HTTP 404` and the url, and does NOT leak headers.
5. **pr_resolver — `_pr_head_refspec` None tail (L384):** add `TestPrHeadRefspec` asserting `_pr_head_refspec` returns None for a `local` record, and for a remote record lacking the needed field (e.g. github with empty pr_id, bitbucket with empty head_ref). Also assert the positive cases return the expected refspec (pins the whole function).
6. **validate_marketplace — skills-loop continue (L182):** new test: build an isolated tmp marketplace+plugin (reuse `run_on_frontmatter("skill", ...)`) whose SKILL.md has a MISSING frontmatter block (no leading `---`). Assert validation fails, the reported error names the SKILL.md missing-frontmatter, and — key for the branch — that the validator does not crash (the `continue` skips the None fm). Since the skill-name/dir check and require_keys are AFTER the continue, a None fm must skip straight past them.
7. **validate_marketplace — agents-loop continue (L202):** same shape with `run_on_frontmatter("agent", ...)` and an unterminated frontmatter block; assert fails + names the agent file + no crash.

## Verification
- `python3 scripts/measure_coverage.py` — both target files' coverage must RISE and the gate must stay PASS (all floors met). Record before/after.
- Full suite green (measure_coverage runs the whole suite under trace).

## Constraints (from dispatch)
- STDLIB ONLY (unittest + unittest.mock). Match existing `mock.patch("pr_resolver.<...>")` and tmp-dir fixture conventions.
- Add tests only. Do NOT modify `pr_resolver.py` (already has `main(argv=None)`) or `validate_marketplace.py`. Do NOT touch `measure_coverage.py`/`coverage_omit.txt`/`validate.yml`.
- Never hit the real network/CLI; never mutate the live checkout (all fixtures under TemporaryDirectory).
