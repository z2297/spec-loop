<!-- spec-loop: risk-tier=2 review="pr-review-toolkit:review-pr" simplify="pr-review-toolkit:review-pr simplify" blocking-bar="P0,P1" surface="scripts/test_dashboard_launcher.py (tests-only; exercises dashboard_launcher.py Layer-B side-effect shell branches)" -->

# Plan — s5: close the scripts/dashboard_launcher.py coverage gap (65.7% -> above floor)

## Goal

Raise runtime line coverage of `scripts/dashboard_launcher.py` (baseline 65.7%,
157/239, floor 63%) by adding tests for the uncovered branches of the thin
subprocess shell (Layer B) — using ONLY the established
`mock.patch("dashboard_launcher.subprocess.run")` argv-recording-fake pattern
already in `scripts/test_dashboard_launcher.py`. STDLIB ONLY (unittest +
unittest.mock). NEVER invoke real docker; NEVER call `_run` unmocked.

## Scope constraints (hard)

- Files touched: `scripts/test_dashboard_launcher.py` ONLY.
- Do NOT modify `dashboard_launcher.py` (prefer tests-only; escalate-gate-log if a
  behavior-preserving seam were ever unavoidable — not expected here).
- Do NOT edit `measure_coverage.py`, `coverage_omit.txt`, or `validate.yml` (s7's
  job). The unreachable `if __name__=="__main__"` shim (lines 551-552) is ALREADY
  in `coverage_omit.txt` (added in s1 — per skeptic), so no new OMIT entry is
  pending; the operative constraint is simply do NOT edit the shim or the manifest.
- Preserve every existing behavioral security-invariant assertion. ADD coverage;
  never weaken.

## Uncovered runtime-body branches to close (from stdlib-trace diagnosis)

Import-time / def-header / module-const lines (2, 38-66, def headers, 278-289,
431, 551-552) are structurally un-traceable by the gate's runtime methodology and
are NOT test targets. The genuinely coverable runtime gaps are:

1. `_port_bound` line 358 — the `"bind for"` disjunct (a bind-for stderr).
2. `_handle_run_failure` lines 400-401 — the generic "docker run failed" branch
   (a run failure that is neither name-conflict nor port-bound).
3. `_execute_plan` line 418 — empty-argvs guard returns `_fallback(...)`.
4. `_execute_plan` lines 422-424 — a non-final (build/stop/rm) step fails with a
   REAL (non-"No such container") error -> abort with `(1, False)`.
5. `read_mount_set` lines 446-451 — FileNotFoundError (returns None) and
   corrupt/non-list payload (warns + returns None).
6. `_run_plan` lines 502-503 — REUSE branch prints "already running" + returns 0.
7. `_run_plan` line 507 — FALLBACK/empty-plan belt-and-suspenders returns
   `_fallback(...)`.

## TDD steps (append new test classes to the existing file; keep existing tests intact)

Each step: write the failing test(s) first, run just that test, watch it pass,
then re-run the full file. All use the recording fake or pure-data drivers.

- **Step A — `_port_bound` "bind for" disjunct (pure, no mock).** [TRIMMED per
  pragmatist/architect/skeptic]
  Assert `_port_bound("... Bind for 127.0.0.1:8787 ...")` is True — this is the
  ONLY newly-coverable predicate line (358). The other `_port_bound` disjuncts
  ("port is already allocated" / "address already in use") and `_name_conflict`
  are ALREADY green via existing `test_port_bound_gives_actionable_message` (357)
  and `test_name_conflict_reuses...` (350) — do NOT re-assert them. Pure string
  predicate, no subprocess.

- **Step B — `_handle_run_failure` generic branch ONLY (pure-ish, `_proc`).**
  [TRIMMED per pragmatist/architect/skeptic]
  Call `dl._handle_run_failure(_proc(returncode=1, stderr="some other error"),
  port)` — a stderr that matches NEITHER `_name_conflict` (checked first, line 393)
  NOR `_port_bound` — assert it returns 1 and prints "docker run failed" to stderr
  (closes 400-401, the sole genuinely-uncovered branch). Do NOT re-cover the
  name-conflict (returns 0) / port-bound (returns 1) return paths — they are
  already exercised end-to-end through `main` (existing tests). No docker.

- **Step C — `_execute_plan` empty-argvs and real-step-failure branches.**
  Drive `dl._execute_plan` DIRECTLY (not via `_run_plan`, whose line-506 guard
  makes 418 unreachable through the normal path — per architect) with a
  `mock.patch("dashboard_launcher.subprocess.run")` recording fake:
  * empty `argvs` -> neutralize the `os.execvp` process-replacement hazard by
    `mock.patch.object(dl, "_fallback", <sentinel>)` (the file's line-512
    module-attr convention — per historian/architect); assert `_fallback` called
    and `ran_ok` False (closes 418), AND assert the recording fake recorded ZERO
    `["docker","run",...]` argvs (guardian: pin the never-invokes-docker invariant).
  * a two-step plan whose FIRST step returns rc=1 with a real error (not "No such
    container") -> assert `(1, False)` and the "<cmd> failed" stderr (closes
    422-424); assert the final `docker run` argv was NEVER run.

- **Step D — `read_mount_set` absent + corrupt.**
  Reuse `TestRegistry`'s `tempfile.TemporaryDirectory` setUp/tearDown lifecycle
  verbatim (per guardian — never write to the real `~/.spec-loop/dashboard`):
  * absent file -> `read_mount_set` returns None, silent (closes 446-447).
  * corrupt JSON -> returns None AND asserts the "unreadable" stderr warning is
    emitted, matching `read_registry`'s observability contract (closes 448-451).
  * non-list payload (e.g. a dict) -> returns None.
  * round-trip: `write_mount_set` then `read_mount_set` returns the list.

- **Step E — `_run_plan` REUSE + belt-and-suspenders FALLBACK.**
  * `dl._run_plan((dl.REUSE, []), desired=[...])` under a recording
    `mock.patch("dashboard_launcher.subprocess.run")` fake -> prints "already
    running", returns 0 (closes 502-503), AND assert ZERO docker calls recorded
    (guardian: REUSE-returns-0-without-subprocess is a real invariant worth pinning).
  * `dl._run_plan((dl.FALLBACK, []), desired=[...])` with
    `mock.patch.object(dl, "_fallback", <sentinel>)` (neutralizes `os.execvp`) ->
    asserts `_fallback` invoked (closes 507), AND assert ZERO `["docker","run",...]`
    argvs recorded (guardian).

## Verification (behavioral success criterion — per skeptic)

Success is NOT "above the 63% floor" (already met at 65.7% before any work). It is:
all coverable runtime-body branches executed — **358, 400-401, 418, 422-424,
446-451, 502-503, 507** — leaving only the structurally-untraceable
import/def-header/module-const lines and the OMITted 551-552 shim un-hit.

- `python3 -m unittest scripts.test_dashboard_launcher -v` (or discover) — all green.
- `python3 scripts/measure_coverage.py` — dashboard_launcher.py runtime coverage
  rises (targeted lines now executed), stays above its 63% floor, total floor stays
  green; full 193+ suite still passes.
- Re-run the stdlib-trace uncovered-line diagnosis: confirm none of the 7 targeted
  ranges remain in the uncovered set.

## Guardian invariant (unchanged)

All existing security-invariant assertions (loopback `127.0.0.1:PORT:PORT` publish,
`:ro` mounts, `--cap-drop ALL`, no docker.sock, scoped name-only teardown) remain
untouched. New tests only ADD branch coverage of the side-effect shell.
